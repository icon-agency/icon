uuid: 14bce19b-5f0f-404b-b983-c0de046caf48
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_work_tile_wide
    - media.type.image
    - media.type.video
    - node.type.work
id: node.work.field_work_tile_wide
field_name: field_work_tile_wide
entity_type: node
bundle: work
label: 'Tile (wide)'
description: 'Optional: the tile for the LANDSCAPE frames — the wide feature and the 3:2 pairs on /work and the homepage. An image or a looping film, 16:9, at least 1920px wide. The square Tile is used where this is empty.'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:media'
  handler_settings:
    target_bundles:
      image: image
      video: video
    sort:
      field: _none
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
