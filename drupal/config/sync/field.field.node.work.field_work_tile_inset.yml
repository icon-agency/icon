uuid: 6ae08b3a-066c-41d0-9305-6b3bdec9cb81
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_work_tile_inset
    - node.type.work
id: node.work.field_work_tile_inset
field_name: field_work_tile_inset
entity_type: node
bundle: work
label: 'Foreground inset'
description: 'How far in from each edge the foreground sits, so 22% leaves it 56% of the tile wide (50% would leave nothing; values over 40% are capped). Leave empty for the default.'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
