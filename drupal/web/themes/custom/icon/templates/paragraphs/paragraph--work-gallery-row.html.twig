{#
/**
 * @file
 * Paragraph "work-gallery-row" = the work-gallery SDC, props from icon_preprocess_paragraph().
 * (In the case study body the field template renders these itself, grouping
 * runs and galleries; this stands alone anywhere else a paragraph renders.)
 */
#}
{% if sdc_props is not empty %}{{ include('icon:work-gallery', sdc_props, with_context = false) }}{% endif %}
