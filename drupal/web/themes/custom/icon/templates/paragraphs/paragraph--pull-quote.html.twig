{#
/**
 * @file
 * Paragraph "pull-quote" = the pull-quote SDC. Props come from icon_preprocess_paragraph()
 * (docs/drupal-mapping-pattern.md: paragraph type → preprocess → thin SDC
 * Twig). No wrapper element — the article flow's CSS keys on the SDC's own
 * root as a DIRECT child of .news-article__flow.
 */
#}
{% if sdc_props is not empty %}{{ include('icon:pull-quote', sdc_props, with_context = false) }}{% endif %}
