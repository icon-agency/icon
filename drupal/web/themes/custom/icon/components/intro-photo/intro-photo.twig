{# intro-photo — a filmstrip photo card (components/filmstrip.css .filmstrip__card). #}
<figure class="filmstrip__card"><img src="{{ image.src }}" alt="{{ image.alt }}"{% if image.width %} width="{{ image.width }}"{% endif %}{% if image.height %} height="{{ image.height }}"{% endif %} /></figure>
