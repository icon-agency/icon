{# intro-photo — a filmstrip photo card (templates/homeC.html .intro__card). #}
<figure class="intro__card"><img src="{{ image.src }}" alt="{{ image.alt }}"{% if image.width %} width="{{ image.width }}"{% endif %}{% if image.height %} height="{{ image.height }}"{% endif %} /></figure>
