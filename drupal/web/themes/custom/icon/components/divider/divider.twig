{#
/**
 * @file
 * divider — the Line (src/components/divider.css): a rule that draws in
 * from the left on arrival, [data-rule] being utilities/rules.css's hook.
 */
#}
<hr class="divider" data-rule="top" />
