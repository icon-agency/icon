# Drupal Handoff

The index for porting this prototype into a **vanilla Drupal 11+** custom theme with **Drupal Canvas** (Single Directory Components / SDC). This is a plain theme — not GovCMS, not Mercury, not a subtheme.

This page is the overview. The mechanics live in two siblings:
- `drupal-mapping-pattern.md` — the worked three-piece pattern (paragraph type → preprocess → thin Twig inside an SDC folder).
- `field-naming.md` — the BEM-prefixed field machine-name rule.

## Goal
Ensure all front-end work translates cleanly into a Drupal theme using SDC and paragraph-based content structures. Tailwind v4 is the build system; **BEM is the public class API**; **CVA / `html_cva()` is not used**.

## Template philosophy
- The BEM components under `src/components/` map 1:1 to SDC components in the theme. Same block name across the CSS file, the SDC folder, and the field prefix.
- Drupal outputs data into **thin Twig templates** inside SDC folders — variable output, loops, and presence `if`s only.
- No logic-heavy template assumptions. Conditional class lists are computed in **preprocess** and emitted as one `modifier_class` string, never built inline in `class="…"`.
- Inline `<script>` from the prototype becomes a Drupal library attached via `Drupal.behaviors` — the JS in `js/` is already authored as IIFEs that map cleanly onto behaviours.

## Preferred mapping pattern
For each UI pattern, document:
- Suggested paragraph type or content structure.
- Expected fields — BEM-prefixed machine names (`field_<block>_<element>`), editor-friendly labels.
- Optional fields.
- Variant flags — a single `list_string` field on the paragraph, not separate paragraph types.
- Any preprocess variables required, especially the computed `modifier_class`.

The full worked example (paragraph YAML → preprocess PHP → `.component.yml` → `.twig`) lives in **`drupal-mapping-pattern.md`** — don't re-derive it here.

## Example handoff note: tagline component

A short, real component to show the **shape** of a per-component note. Source: `src/components/tagline.css`, `js/tagline.js`. (The tagline itself now lives only on the prototype pages — the canonical components carry richer notes of this same shape in the design-system index (`/index.html`), e.g. the Intro section's "Drupal (Canvas / SDC)" block, which lists props, behaviours and reduced-motion notes ready to lift into a `component.yml`.)

Suggested Drupal structure:
- Paragraph: `paragraph: tagline` (page-section paragraph).
- Fields:
  - `field_tagline_heading` (text long — the fit-to-width statement)
  - `field_tagline_pops` (entity reference / media, multiple — the cursor-pop trail images)

Preprocess notes:
- Map the heading and the image list to clean scalar/array vars; null-check entity references with `?->`.
- Run `t()` on any static labels.
- If a variant is ever added, compute `modifier_class` here — there is no variant today, so the root class is just `tagline`.

Twig notes:
- Markup mirrors the `src/components/tagline.css` BEM structure: `.tagline` → `.tagline__heading` (with `.tagline__line` children) and the `.tagline__pops` / `.tagline__pop` trail container.
- Emit the root as `<section class="{{ modifier_class|default('tagline') }}">`. No inline conditionals, no `html_cva()`.
- The fit-to-width sizing and InertiaPlugin cursor-pop trail come from `js/tagline.js`, attached as the `icon/tagline` library via `Drupal.behaviors` — not from Twig.

## Field naming guidance
Machine names are BEM-prefixed; labels stay human. See **`field-naming.md`** for the full rule and the exceptions (core fields, media fields, entity references).

## Handoff note block
Every component note should include:
- Drupal structure suggestion (paragraph type or SDC).
- Field list with machine names + labels.
- Preprocess notes (including any `modifier_class` computation).
- Twig notes.
- Known implementation risks (e.g. animation that must move into a `Drupal.behaviors` library).

## What ports vs. what is rebuilt

| Layer | Action |
|---|---|
| `src/theme/*.css` (tokens) | Port as-is into the theme's `src/theme/` directory. |
| `src/base/*.css` | Port as-is. |
| `src/utilities/*.css` | Port as-is. |
| `src/components/*.css` | Port as-is. The BEM names match what the SDC Twig outputs. |
| `templates/*.html` | Rebuild as SDC `.twig` under `components/<name>/<name>.twig`, plus a `<name>.component.yml` schema. `homeC.html` (canonical), `news-b.html`, `work-landing-b.html` (the header-B winners; the A variants stay as alternates) the news-article templates and the work articles (`work-article-master.html` is the reference; the client folios are instances of it) are the pages being ported; `home`/`homeB`/`work` are prototypes — port their components only where the system has adopted them. |
| `js/*.js` | Move into Drupal libraries; attach via `Drupal.behaviors`. The full behaviour → library table (header, reveal, home, site-footer, news, work, work-filter, work-landing, theme-handover, share, subscribe-reveal, velocity-lean, cursor-tilt, team-profiles, work-article, work-scroller, work-video, plus the prototype-only files) is in `docs/animation.md`. `js/hero-loader.js`'s generated rows become a Twig loop inside the hero SDC. |

The CSS doesn't change shape across the port — only the markup gets re-templated as Twig + SDC schemas.

## The theme

`drupal/web/themes/custom/icon` (the DDEV project is `drupal/`, docroot `web/`; see the README). `base theme: false`, two regions (`highlighted` for messages, `content`), Claro stays the admin theme. Decisions, each one a rule:

- **One build, not a copy.** The theme's `src/main.css` `@import`s the repo-root `src/main.css` unchanged and adds `@source "../templates"` + `@source "../components"`, so the Twig and SDC markup feed the same utility scan. `npm run build:theme` writes `css/main.css` (minified, committed — Drupal serves the committed file). The root `@source` lines stay until every page is Twig, so the theme output is a superset of the root build meanwhile. The root entry fences the site off with `@source not "../drupal"` — Tailwind's automatic source detection would otherwise scan the theme into the STATIC build (LESSONS.md). Drupal-only pages the static system has no template for (the account pages, `templates/layout/page--user.html.twig`) get their CSS as the theme's own page layer, `src/auth.css`, imported after the root entry under the same rules; `src/admin-chrome.css` shifts the fixed chrome by Drupal's `--drupal-displace-offset-*` so an editor's admin top bar and sidebar stay clickable (the chrome's z-index sits above them).
- **Behaviours are generated, never forked.** `scripts/theme-js.mjs` wraps each root `js/*.js` IIFE verbatim in `Drupal.behaviors.<name>.attach()` (gated by `once()` on `<html>`, so an AJAX re-attach is a no-op) and writes the theme's `js/`. `velocity-lean.js` and `hero-loader.js` are copied as plain IIFEs (the first only defines `window.ICON.velocityLean` — consumers declare the `icon/velocity-lean` dependency; the second must run after the hero's markup and before the CDN scripts, so its library is footer-scoped with a negative weight — NOT `header: true`, which runs it before the body exists; LESSONS.md). Prototype-only behaviours (hero, tagline, hero-sphere) are not ported. `icon.libraries.yml` is the `docs/animation.md` table, one library per behaviour; `icon.info.yml` attaches the chrome set (global CSS + fonts, header, reveal, velocity-lean, site-footer) on every page.
- **The footer's content is the CMS's.** `site-footer.html.twig` keeps its markup but reads three things from `icon_preprocess_page()` (via icon_site, with every cache tag riding along in `footer_cache`): `footer` — the call to action and its link, the newsletter line and the acknowledgement from **Content → Footer** (`icon_site.footer` config, `FooterSettingsForm`) plus `footer.social` from the **footer-social menu** (add, edit, drag, delete are Drupal's own menu UI, linked from the Footer page); and `offices` — published **Office** content in the order of **Content → Offices** (`OfficesForm`, a tabledrag over `field_office_weight`; Add office, Edit, Delete). An office is city (title), traditional place name, address (one line per row), phone (the link dials it, `0…` → `+61…`), email and an **icon** picked with the media library widget from the Icon bundle — the same SVGs as the fact cards, now listed as **Content → Icons** — or uploaded new in the same dialog (`icon_site_preprocess_media()` inlines the SVG in the library grid, whose thumbnail would otherwise be the generic file icon). `scripts/offices-content.php` seeds the four offices, their icons (`sample-content/icons/office-*.svg`), the words and the two social links. Canvas registers a menu block component for every new menu; `canvas.component.block.system_menu_block.footer-social` is exported with `status: false` so it stays out of the library.
- **Chrome is Twig, not regions.** `templates/includes/site-header.html.twig` and `site-footer.html.twig` are the static header/footer with the four home links at `path('<front>')`, labels through `|t`, and the footer's ICON mask playing the HOMEPAGE HERO'S OWN SLIDES (8 Sep 2026: `footer_slides` from `icon_preprocess_page()` via `icon_site_hero_reel()` on every page, films as `<video preload="none">`, stills as lazy `<img>`, at the very same URLs the hero renders — same image style, raw film — so the browser serves them from cache once the hero or an earlier footer has played them; the theme's `assets/banner/` films are only the fallback for a site with no slides). The one asset the CSS itself references — `url(../assets/brand/logo-dark.svg)`, the footer's logo mask — resolves relative to `css/main.css`, so it is mirrored at `assets/brand/` in the theme; any new `url(../assets/…)` in `src/` needs the same mirror. `html.html.twig` adds the `js-animations` class inline before the stylesheet; `page.html.twig` is skip link → header → `<main id="main">` → footer, with `main_attributes` (from `icon_preprocess_page()`) as the hook for a page layer's shell class. The theme class (`dark`, `theme-work-article`) is added to `html_attributes` by page preprocess, never to `<body>`.
- **Search lives in the pill (8 Sep 2026).** The nav's sixth item is a search glyph; clicking it rolls the pill over like a drum (`.site-nav__cube`, perspective on `.site-nav`, both faces sharing `.site-nav__pill` — the Runway header flip, `site-header.css` "Search flip") to an identically shaped search face: glyph on the left as the submit, the field, and a close X sitting exactly where the search glyph was (same padding, same box, last item of a row in a face of the same width). The pill also frosts on hover at the top of the page (once scrolled the background is already there). The form posts to core's content search — Drupal 11.4 moved that into the `search_node` module (the old `node` plugin denies everything; LESSONS.md) — at `path('search.view_node_search')` (`/search/node?keys=`); `search content` is granted to anonymous and authenticated, the user search page and Canvas's auto-registered search-form block are disabled, and cron indexes 500 nodes a pass. `js/header.js` owns the open state, measures `--nav-depth` (half the pill's rest height) from the row, closes the SERVICES drawer first, swaps `inert` between the faces and hands focus into the field and back to the glyph. The results page is core markup inside the theme shell — unstyled, a later slice. Mobile has no search yet (the pill is desktop-only; the full-screen menu is the place for it).
- **One pill at every width (8 Sep 2026, a trial).** Below md the same pill nav is fixed at the BOTTOM of the viewport in place of the hamburger, the contact button, the mobile home button and the full-screen menu, which are parked by one `display: none` rule in `site-header.css` ("Parked mobile chrome") — their markup, rules and behaviour stay so the trial reverses with that rule, and come out if the pill sticks. Everything in the pill is sized in em off `--nav-voice`, which below md is `min(label voice, (100vw − gutters) / --nav-row-em)`: the row measures 30.75em at rest with the home glyph showing (`--nav-row-em: 31`, re-measure if the links change), so the whole row always fits, down to a 320px phone (9.5px type there — the trade the user asked for); below md the items sit at 0.4em side padding, which is what buys that size. The nav reads WORK · EXPERTISE · ABOUT · NEWS · CONTACT · search (8 Sep 2026: AGENCY became ABOUT and WORK leads), and the home glyph is 1.5em at every width — the search glyph's size. The pill is always frosted below md (it sits over content), the EXPERTISE drawer opens upward (`column-reverse`, same drawer, same JS alignment), the search flip is unchanged, and the footer pads its last edge by `--nav-clearance` so the pill never sits on the logo. Hover-open and hover-frost rules are inside `@media (hover: hover)` (LESSONS). From md every size returns to the rem values the desktop was tuned at and the pill goes back to the top. Below md the wordmark is `position: absolute` — part of the page's top, scrolling away with it (the pill's home glyph takes over from 100px), with the masked slide-out desktop-only — after a report that it could not be seen on a phone; and the homepage hero's client name is centred, in the label voice, lifted by `--nav-clearance` so it clears the pill. The /news filter band's air is equal above and below the chips (the header's own bottom padding goes when the band is the open kind). Not yet handled: `js/home-c.js` flags "over hero" from the TOP of the viewport, so on a phone the bottom pill can be inverted while it is over the content below the hero — a root-margin change if the trial sticks.
- **Still static, by design, until their slice lands:** the nav items and service lists (a menu-driven header is a later slice), `aria-current` on the active nav item, the footer newsletter inputs (the webform/newsletter block), and the header trigger reads EXPERTISE in every template (reconciled 8 Sep 2026 with the reorder).
- **Verify covers it.** `npm run verify` check 5 byte-compares the theme's `css/main.css` and every generated `js/` file against a fresh build.

## News — the first content slice (4 Sep 2026)

What `templates/news-b.html` and `news-article.html` became, and the decisions worth knowing before the Work slice repeats the shape.

- **Content type `news`** (`drupal/config/sync/node.type.news.yml`): `field_news_image` (media image, required — the square tile), `field_news_banner` (media image, optional — the 2:1 banner whose presence picks the head variant, exactly the rule `news-article.css` prototypes: present → the stacked head with the wide hero, absent → the tile head; no variant field), `field_news_category` (`list_string`, keys = the `?category=` slugs the static chips already used — `agency`, `our-work`, `awards`, `insights`), `field_news_date` (date), and `field_news_content` — a Paragraphs field (`entity_reference_revisions`) holding the body. Field names follow `field-naming.md` with `news` as the shared block (card and article both read them).
- **The article is a node template; the body is Paragraphs, each rendered by an SDC.** `node--news--full.html.twig` is the fixed frame (back link, title, category / date row, tile photo or banner, both Share rails, the Next up rail) and `{{ content.field_news_content }}` the composable middle. Four paragraph types — `prose`, `news_article_figure`, `news_article_video`, `pull_quote`, fields BEM-prefixed (`field_prose_text`, `field_pull_quote_variant`, …) — go through `icon_preprocess_paragraph()` into `sdc_props` and a one-line `paragraph--<type>.html.twig` includes the SDC of the same name: the three-piece pattern of `drupal-mapping-pattern.md`, literally. `field--node--field-news-content.html.twig` prints the paragraphs bare so the flow's direct-child measure rule holds. Editors get the classic Paragraphs widget on the node form (modal add, closed summaries).
- **Why not Canvas for the body (4 Sep 2026).** Canvas 1.10.1's composer opens only its own Pages: `ComponentTreeLoader::getCanvasFieldName()` throws for every other entity type ("Other entity types and bundles must use content templates for now", issue 3498525, still open, no newer release). Content Templates DO work (`/canvas/template/node/news/full` composes the full-view layout once, binding SDC props to node fields) but per-story composition inside a template ("exposed slots") has no editor UI yet. So the tree field was retired and the body moved to Paragraphs; the four SDCs are unchanged and Canvas-eligible, so a later move is a data migration (paragraph → tree item), not a rebuild. Canvas keeps the homepage (a Page) and can own the article FRAME as a Content Template later.
- **Four body SDCs** (`components/`): `prose` (rich text → `.content-page`; from Paragraphs the text arrives as a `processed_text` render array in format `basic_html`), `news-article-figure` (Canvas image prop → `.news-article__figure`), `news-article-video` (an embed URL + title → the 16:9 film band), `pull-quote` (`variant` enum italic/upright, optional `cite`). The prose SDC cannot know which article it sits in, so `src/utilities/news-article.css` now also gives the measure to `.news-article__flow > .content-page` — the one design-system CSS change this slice needed.
- **The card is an SDC** (`news-card`: title, url, Canvas image, category, date, `heading_level` enum h2/h3). The node teaser (`node--news--teaser.html.twig`) is one include of it with props from preprocess; there is no `<article>` wrapper because `news-card.css` establishes its container query on the card's direct parent. The rail sets h3 through `#news_heading_level` on the row build **and** a `#cache` key (LESSONS.md).
- **The listing is one View, `news`**: `page_1` at `/news` (12 per page, sorted by `field_news_date`), with a contextual filter on the category whose default is the **query parameter** `category` (fallback `all` = the exception value) — so the chips are real links that filter server-side, exactly the no-JS baseline `filter-bar.css` describes, and `js/news.js` enhances them in place. On a `?category=` page the `data-news-list` hook is withheld, so the in-place filter only ever runs over the full set. `next_up` is a block display (3 latest, current node excluded via the `node` default argument) embedded by `icon_preprocess_node()`. The masthead, chips, list wrapper and pager are `templates/views/` + `templates/navigation/pager.html.twig`, fed by `icon_preprocess_views_view()`, `_views_view_unformatted()` and `_pager()` (the status pill's noun rides on the pager element as `#noun`).
- **Dark opening + behaviours**: `icon_preprocess_html()` adds `dark` to `<html>` on the listing and news nodes and attaches `icon/theme-handover`; the listing attaches `icon/news` + `icon/subscribe-reveal`, the article `icon/news` + `icon/share`.
- **Sample content**: `drupal/scripts/news-sample-content.php` (`ddev drush php:script …`) creates the twelve listing stories (the Butterfly one with the 2:1 banner, so both heads are on the site) and the PRCA article with its body as paragraphs — prose, the film, figures, pull quotes. Re-running replaces each story's paragraphs. Images are the repo assets copied to `drupal/sample-content/news/` (git dedupes identical blobs). (For the record, building a Canvas tree in code works too — a prop on the component's default static prop source is stored **collapsed**: `getDefaultStaticPropSource($prop)->withValue($v)->getValue()` under the prop name, JSON-encoded in `inputs` — it just has no editor for nodes yet.)
- **Loading the archive**: /news pages 10 a page (8 Sep 2026; was 12). The first page and one more arrive on their own as the reader nears the end (`data-lazy-auto="1"` on the list, read by `js-theme/listing-lazy.js`), so the first 20 stories come unasked; after that a "Load more" pill at the foot brings each next 10 on a click, until the status line says that is all. /work keeps every page arriving on its own (no attribute). The pager stays in the markup for no-JS and crawlers.
- **Still open**: Subscribe (the masthead bar and the footer) stays inert until the newsletter slice — both are now the header search face's twin (8 Sep 2026: the shared `components/subscribe-bar.css`, promoted when the footer became its third consumer; the footer wears the `--footer` variant in the footer's fixed tokens; `icon/subscribe-reveal` is attached globally in `icon.info.yml` since the footer is on every page; an envelope glyph, the field hinting "Subscribe" at rest and "Your email" on hover or focus — two overlaid spans in the markup that crossfade and slide (0.3s; the native placeholder is a lone space so `:placeholder-shown` still clears the hints once typed), with `js/subscribe-reveal.js` only holding `.is-hinting` while the bar is under the pointer or the field focused, the typed address in the search field's regular face, the arrow as the submit; every pill is COMPACT at rest (13rem: glyph, word, arrow) and grows on a click into it or while it holds a value (`.is-open` from the behaviour, 0.45s; a click anywhere on the pill focuses the field) to its open width — the search face's 26rem on the mastheads, the column's full width in the footer (`--subscribe-bar-open`), where the blurb keeps its own three-line 20ch measure inside the wider column; a `<div>`, not a `<form>`, so Enter cannot submit to nothing yet; the same markup on the news and work landings); the local tasks block sits unstyled in `highlighted` so editors can reach Edit; the `basic_html` toolbar has not yet been trimmed to what `.content-page` styles (`wysiwyg-output.md`).

## Work — the landing and the case study (4 Sep 2026)

What `templates/work-landing-b.html` and `work-article-master.html` (+ the client folios) became. The News shape, repeated — the differences are the ones the design system itself names.

- **Content type `work`**: `field_work_client`, `field_work_category` (`list_string`, multiple — the six landing slugs; the FIRST value is the card's filter key, all are listed in the facts row), `field_work_deliverables` (multiple strings), `field_work_statement` (the serif statement under the banner), `field_work_tile` (media image OR video — the card), `field_work_banner` (media image or video, optional, falls back to the tile), `field_work_bg` + `field_work_ink` (the chameleon pair as hex strings, defaulting to the master's blue / white), and `field_work_content` (Paragraphs).
- **The chameleon is preprocess, as the CSS asked.** `icon_preprocess_html()` puts `.theme-work-article` and the two custom properties inline on `<html>` for a work node (hex-validated), and attaches the handover; the banner in `node--work--full.html.twig` carries `data-theme-handover="gone"` + `data-theme-handover-class`, so the page hands back to the site theme as the banner leaves — exactly the static templates' hooks.
- **The body: seven block types.** The shared `prose` (Content), `pull_quote`, `news_article_video` (Video) plus `work_gallery_row` (Gallery row: one or two nested `work_gallery_figure` **Figures** — 8 Sep 2026, so a pair of the old folio's square layered tiles is one row; each Figure carries 1–2 media, a style of plain / portrait / layered (16:9) / layered square (1:1), and for the layered ones an optional ground colour + float inset; a layered figure's FIRST media is the foreground that floats and its LAST the background — editors think front-to-back, so `_icon_work_paragraph_props()` reverses the pair for the component, which wants the ground first — or one item floats on the colour), `work_video` (**Video hosted** — a film file as media + cover image + title), `work_scroller` (**Gallery scroller**: images + ground colour) and `work_stats` (**Results and stats** → nested `work_stat` Result: value / unit / label); `news_article_video` is **Video embed (YouTube, Vimeo)** everywhere it appears, and `work_gallery_row` is simply **Gallery** (all renamed 8 Sep 2026). The Add Block dialog lists them in four groups — Content, Pull quote / Gallery, Gallery scroller / Video embed, Video hosted / Results and stats — by `icon_site_field_widget_complete_form_alter()`, which wraps the widget's buttons (`css/block-picker.css`); the order is the field's `target_bundles_drag_drop` weights. Each renders through the SDC of the same name (`work-gallery`, `work-video`, `work-scroller`, `work-stats`); `_icon_work_paragraph_props()` owns the lookups.
- **Runs and galleries are composed in the field template.** `icon_preprocess_field()` turns the paragraphs into `groups`: consecutive Content / Film / Video / Results blocks become ONE `.work-article__chunk` with one Share rail (the master's "run"); consecutive Gallery row blocks become ONE `.work-gallery`; a Scroller or Pull quote stands alone in the flow. Editors just order blocks; the rails and galleries fall out of the sequence. `src/utilities/work-article.css` gives a bare `.content-page` in a chunk the prose measure, as the news layer does.
- **The card is an SDC** (`work-card`: title, url, client, media {type, src, alt, width, height}, heading level, caption delay) whose root is the homepage's `.work__item`. Media helper `_icon_media_source()` returns an image (through the `work_media` style) or a video file, so a tile or banner can be a looping film.
- **The landing is the `work` View**: `/work` with the same query-parameter category filter as `/news`, 12 per page, sorted by authored date; `next_up` (2 latest, current excluded) is the rail. The masthead, chips, uncapped grid (`views-view-unformatted--work.html.twig`, `data-work-list` withheld when server-filtered) and pager ("projects") are theme templates; `icon/work-landing` + `icon/subscribe-reveal` attach on the listing, `icon/share` + `icon/velocity-lean` + `icon/work-article` + `icon/work-video` + `icon/work-scroller` on the case study.
- **Sample content**: `drupal/scripts/work-sample-content.php` — the twelve landing projects; iCanQuit, The Athlete's Foot, Melbourne Marathon (Nike) and MoAD carry bodies after their folios and pairs; media in `drupal/sample-content/work/` (renamed kebab-case; git dedupes the repo's copies).
- **Still open**: the ground colour and float inset are still free text that reaches inline CSS (foundation decision 6: presets); the layered banner variant (`.work-article__banner--layered`) isn't reachable from the editor yet (the `list-slash` prose option is, since 8 Sep 2026: a Styles dropdown in the Content block's editor with one entry, Slash list, which puts the class on a bulleted list — `basic_html` allows `<ul class="list-slash">`); `work_gallery` SDCs with array-of-object props are not Canvas-eligible (Canvas has no shape for them), which is fine — they are Paragraphs-fed.

- **A film can carry a Poster (8 Sep 2026)** — an optional image on the Video media type (`field_media_poster`, in both the full and the media-library add forms). `icon_site_media_presave()` makes it the media's thumbnail, so the library and every admin list show the still instead of core's play glyph; `icon_site_media_source()` returns it as `poster` (at the caller's image style) and every `<video>` a source feeds — hero slide, work card, gallery figure, footer mask — wears it as its `poster`, the frame shown before playback. Automatic frame grabs would need ffmpeg on the server; the poster is the dependable path and the alt-text generator can read it.
- **The media folder is required, and pre-selected from the page** (8 Sep 2026): `field_media_category` is labelled **Folder to save in** on image and video media, required, with no stored default (it was `content`), and the library's filter is labelled Folder. `icon_site_form_media_library_add_form_alter()` reads the library opener's bundle through `_icon_site_media_folder_for()` — a Work page or any `work_*` block (gallery figure, scroller, film) → work, a News story or `news_*` block → news, a Hero slide → hero — and pre-selects it on each new item; where nothing can be inferred (a shared prose or pull-quote block, the library itself) the select opens on its blank prompt and the editor must choose.
- **A Work item's tile comes in two shapes, with a foreground (8 Sep 2026).** On the Work form they are one open **Tile** block, two up (`_icon_site_work_tile_group()`, css/work-tiles.css): **Square** (1600px); an optional **Wide** is the 16:9 media for the full-width feature frame only (since 10 Sep 2026 the split pairs are two squares at every grid width — they were a landscape beside a square from 900, and read as a mismatch) — and an optional **Foreground** (an image, with a **Foreground inset**, e.g. 22%) floats over either, centred, the way a gallery figure's float does (`.work__float`, `--overlay-pad`). The card (`work-card.twig`) carries both tiles when there are two; the frame's CSS picks — the feature rule sets `--tile-wide: block`, every other frame `--tile-square: block` (home-c.css, work-landing.css) — so the choice follows the nth-child pattern and the filter, not a server guess. A hidden image is lazy and never fetched; a hidden film's `src` waits in `data-src` until `js-theme/work-tile-variants.js` sees which variant shows (attach, the 768/900 crossings, `hidden` toggles). Running Wings carries the Nike banner film as its wide tile and the lockup as its foreground.
- **A Work item's title IS its project name (8 Sep 2026)** — short, "Raise It" — labelled Project name on the form (a base-field override carries the label and help). It is the case-study `h1`, the start of the URL and, with the client, the page title; the CARD's headline is the **project name** too (user call, 11 Sep 2026 — for a while it was the Feature statement, the outcome in one line; the statement stays the serif line under the banner) — the form runs Project name, Client, Feature statement, Tile, Legacy folio card, Banner: `icon_preprocess_html()` writes "Raise It for PHN North Western Melbourne — ICON Agency" for `work` nodes so the tab and the search result name the client. The separate Project name field is gone; the sample script seeds the short names as titles (its headline sentences stay in its data as `headline`, unused) and matches existing items by title.
- **URLs are Pathauto's** (8 Sep 2026: `pathauto`, `redirect`, `token`). One pattern so far, Work: `/work/[node:title]-[node:field_work_client]` → `/work/raise-it-phn-north-western-melbourne`. Pathauto's stop-word list is emptied (`pathauto.settings` `ignore_words`), so "Fit for Every Run" keeps its *for* and "Is This Legit?" its *is this*. The twelve seeded work URLs were regenerated and each original (`/work/raise-it`…) is a 301 in Redirect to its node, so nothing already shared breaks. No News pattern yet — the 270 mirrored stories keep their old-site URLs, which matter for launch; a pattern there would only apply to new stories, and Redirect will catch any alias that changes. At launch the old site's URLs that differ from ours want redirects of their own.

## Homepage — a Canvas Page (4 Sep 2026)

`templates/homeC.html` as a Canvas **Page** (`/home`, the site front page), composed from five components a site builder can reorder, remove or re-word in the Canvas editor (`/canvas/editor/canvas_page/1`). `scripts/home-page.php` builds it.

- **The news feed is the `News feed (homepage)` block** (`block.icon_news_latest`, icon_site): it renders the news View's `latest` display (promoted stories, pinned first, newest next) with its one setting, the count. Its Canvas panel is the feed itself — each story with a pin toggle (sticky), Edit (the story's form in a dialog over the editor) and Remove (un-promote), and a searchable "+ Add a story to the homepage" that promotes AND pins a story not in the feed (the feed is date-ordered, so an older story only shows pinned). Pin / Remove / Add hit `/icon-site/news-action` (CSRF-protected; `NewsActionController`) and reload the editor. The same flags are on every news form as "Promote to homepage" and "Pin on homepage". The marquee is placed as `block.icon_clients_marquee` (icon_site), which queries every published **Logo** media in Order and renders the `clients` SDC itself (no View); its one setting is the section's accessible label, and its Canvas panel IS the logo list: drag to reorder (writes the Order field at once through `/icon-site/logo-action`), Edit (the media form in a dialog), Remove (unpublish — the file stays in the library), "+ Add logo" (the media add form in a dialog). The Client logos admin page below is the same list. (A Canvas 1.10 gotcha: a block with NO settings gets no client-side model, and opening its panel throws `reading 'source'` — give a block at least one real setting.)
- **The hero and Featured work are blocks whose Canvas panel is a draggable list.** The `Homepage hero` block (`block.icon_hero`, icon_site) renders the `icon:hero` SDC with an `icon:hero-slide` component per **Hero slide** content item (node type `hero_slide`: title = client name, `field_slide_media` = Video or Image media via the media library widget, `field_slide_link`). Its one setting is `order` — since 2026-09-08 an explicit SELECTION: the hero shows only the slides its block chose, in that order (`icon_site_hero_slides()`), and the panel is two lists — the reel (drag to reorder, Edit) and, below it, **Available — not on this reel**: every published slide the reel does not use, each with **Add to reel**. The slide's Edit dialog carries the reel action (user call, Sep 2026: "place the Remove on the edit page") — **Remove from reel** for a slide on the reel, **Add to reel** for one under Available; the panel script sets which when the dialog opens, and the click moves the row and closes the dialog. Every move is client-side and writes the order the way a drop does; a slide created from the panel's own Add dialog goes straight onto the reel (the dialog's save event carries the new id and the sorter appends it before the reload). Publishing a slide no longer changes any hero by itself. Up to eight (`HeroBlock::MAX`). Films must be 6 seconds, muted, loop — on the slide form and the panel. The slides are read through one helper, `icon_site_hero_reel()` (order → published slides → media sources, with the cache tags of everything it read): the block passes its own `order`; called with none it reads the FRONT PAGE's hero block order out of the Canvas page tree (`icon_site_hero_order()`, `system.site page.front` → the canvas_page → the `block.icon_hero` component's `order` input). The login page's reel is NAMED, not discovered: Content → Footer → Login page → "Hero reel to play" (`icon_site.footer` `login_hero`, a Canvas page; the front page by default) — `icon_site_hero_order()` reads that page's hero block selection (`icon_site_hero_order_of()`). That is how the account pages' banner (`page--user.html.twig`, `.auth__reel`, `icon/auth-reel`) plays the same reel — every slide as a `.auth__slide`, a film held six seconds, a still three, the hero's 100ms cross-fade, the client name written bottom-left — so editing or reordering the slides in Drupal updates the login page too; with no slides it falls back to the theme's own film. The two SDCs are hidden from the Canvas library (component status off) so nobody places a second hero; they are rendered only by the block. Canvas 1.10 cannot lock a component in place, so the hero block can still be dragged in the tree — nothing needs to be dragged, but a locked hero would mean placing the block in a theme region with a front-page visibility condition, outside the Canvas tree and without its panel. `Featured work` (`block.icon_featured_work`) is the FIXED five-tile grid: its one setting is `projects` (five node IDs in order) and its panel is a tabledrag table of five rows — a tile label, a select of every published Work item by title, an Edit link to the article (new tab), plus an "All work articles" link; empty picks fall back to the latest. It renders the `icon:featured-work` SDC (hidden from the library) by filling its three row slots — `pair_top`, `feature`, `pair_bottom` — with the picks' teasers, the third marked `#work_wide`. The feature row's tile is also wide by the ROW's modifier (`.work__row--feature`) in the design system. Both panels are card lists reordered by `icon_site`'s own `js/panel-sortable.js` (core tabledrag dies on Canvas's React re-renders, and Canvas ignores weight selects, so the order travels in one hidden text field the sorter writes); Add / Edit open the Hero slide form in a Drupal dialog over the editor (`?panel=1&use_admin_theme=1`, Save closes it and reloads); the featured rows show project name over client and open a searchable dropdown on click (every published Work item, filtered client-side on project or client; the pick is written into a hidden text field as the node ID, which the block validates against published Work on save.php`, which is also what validates it) — and a "Show the latest five automatically" switch at the top (`latest`) that makes the grid the newest five and the list a dimmed preview. Every picker also opens with **Show latest** as its first option (the empty pick, `0`): that tile is the newest work article not placed elsewhere, in its own position — two such tiles are the two newest (user call, Sep 2026). A Work item's own **Title** IS the project name (its label is overridden to "Project name"), and the card's headline (11 Sep 2026). Styled by `icon_site/panel_lists` (`css/panel-lists.css`, attached to the Canvas editor page by `hook_page_attachments()` — a library attached by the block form itself never reaches the panel), and `blockSubmit()` reads the table by its nested parents path because Canvas posts the form with `#tree` semantics. Canvas 1.10's layers panel shows the component name for every instance (the per-instance label is stored and sent — the UI ignores it).
- **The homepage news feed is core's two node flags, relabelled.** `news.latest` shows PROMOTED stories only (`promote = 1`), pinned ones first (`sticky` DESC), then by date. The News form shows them as **Promote to homepage** and **Pin on homepage** (`icon_site_form_node_form_alter()` relabels core's "Promoted to front page" / "Sticky at top of lists"); new stories are promoted by default, so un-ticking removes one from the feed and pinning keeps it at the top whatever its date.
- **Client logos are a `logo` media type** (file source, `svg png`, the name is the alt, an Order field), managed as ONE LIST at `/admin/content/client-logos` (icon_site — also a tab under Content and a menu item): a draggable table of every logo — drag to reorder (writes `field_logo_weight`, the View's sort), rename the alt text inline, Edit (replace the file or rename), Delete, and an "Add logo" action at the top. The reusable piece is the `clients` SDC (`logos` array + heading), which any block or preprocess can feed.
- **The Canvas library is four folders (10 Sep 2026; user call: "I'm getting lost").** `scripts/canvas-library.php` shapes it and is the record of the shape: **Page** (Masthead, Content, Pull quote, Image, Video embed, Filmstrip, Team profiles, Clients marquee, Next up: news, Next up: work), **Work article** (Gallery, Video hosted, Gallery scroller, Results and stats — the Work form's own names), **Homepage** (Homepage hero, Intro strip, Featured work, News feed) and **Parts (go inside a block above)** (Figure, Result, Photo, Fact card, Expertise link). Drupal's chrome blocks and the row components nobody places by hand (Site branding, Breadcrumbs, Messages, Powered by, Search form, the two menus, Message, Title, Teaser, News card) are OFF — hidden from the library, a switch — and every other folder is deleted; the script is idempotent, so run it (then `drush cex`) after a new component registers, and give a new SDC the folder's name as its `group` so Canvas files it there on creation. An item lives in at most one folder (Canvas throws otherwise).
- **The Gallery scroller takes films (11 Sep 2026; user call).** `work-scroller` has a `films` prop (an array of Canvas video shapes) rendered as muted, looping, `playsinline` video cards after the images; the Work form's scroller media field accepts Video media as well as Image, and `_icon_work_paragraph_props()` splits the one ordered field by type through `_icon_media_source()`; `js/work-scroller.js` measures a film's `videoWidth/Height` for the height governor.
- **The Digital expertise page is a Canvas page (11 Sep 2026; user call: "I want to edit /services/digital").** The header's Expertise drawer and the intro's expertise list link to `/services/<name>`, which never existed here; `scripts/digital-page.php` builds `/services/digital` from the live site's words — a dark Masthead, the intro, the eight service blocks two to a row in **Columns** (each a Content block: h3, a line, a slash list), and **Latest work**: the work View's new `latest` block display (`block.views_block.work-latest`, Page folder — the Next up rail with nothing excluded, for any page; `views-view--work--latest.html.twig`). The other five expertise pages follow the same script when wanted.
- **The Content block's editor is the News body's (11 Sep 2026; user call).** Canvas's own `canvas_html_block` format and editor carried a smaller toolbar (Paragraph, H2–H4, bold, italic…); they now match `basic_html`: the same toolbar (bold, italic, link, lists, the Slash list style, block quote, headings, code, source), headings 2–6, list start/style options, and the format allows `<h5 id> <h6 id>` and `<ul class="list-slash">` — minus inline images (the Image component is the Canvas way). No H1: a page's H1 is its Masthead. **Three H2s** in the Styles menu (user call): the caps default (tight-leaded), Heading 2 — Sentence (`h2.is-sentence`, the news article's recipe) and Heading 2 — Miller (`h2.is-serif`), and **Paragraph — Small** (`p.is-small`, the team panel's bio size) in both the Content block's and the News body's editors; `base/prose.css` styles them, the formats allow the classes. **Paragraph — Large** (`p.is-large`, the homepage intro statement's voice: `--voice-mid`, medium, 1.2, page ink) joined them, then **Heading 4 — Caps label** (`h4.is-caps-label`, the facts row's column title: the label voice at 900), **Paragraph — Caps** (`p.is-caps`) and the **Caps list** (`ul.list-caps`), the facts' items at medium (user ask, same day), — and since 11 Sep 2026 the editor WEARS them: `icon.info.yml` declares `css/editor.css` as the theme's `ckeditor5-stylesheets`, built by `npm run build:theme` from `src/editor.css` (the two font links, the tokens, `base/prose.css`) and re-scoped by `scripts/editor-css.mjs` from `.content-page` to `.ck-content`, so the editing area and the Styles menu's previews show each voice (user call: "can they inherit the actual font style?"); the editing area is zoomed to 0.62 and the previews to 0.5, since the page's sizes were written for a page, not a panel — and the **Alignment** tool (left, centre, right, justify — core's `text-align-*` classes, styled in `base/prose.css`, allowed on `p`, `h2`–`h6` and `li`), both user asks the same day. A block placed **under the Masthead heading** reads left and stands `--space-lg` below the lockup (`page-header.css`); the inner's centring is the lockup's alone.
- **The Work article's body is a Canvas field (11 Sep 2026; user call: "Work article could be Canvas, with a Work masthead that includes Category and Deliverables, the rest is builder").** `field_work_canvas` on the Work type, rendered by Canvas's naive formatter inside `.work-article__flow`; the FRAME (eyebrow, project, client, banner, feature statement, facts) stays the node's own fields — `includes/work-frame.html.twig`, `_icon_work_frame()` — because the tiles, the listings and the chameleon read the same fields, and the Canvas editor shows those fields in its Page data panel. **In the editor** the page template composes the frame around the body from the DRAFT node (`_icon_route_node()` reads the auto-save on the `canvas.api.layout.*` routes), the chameleon hands over as `data-icon-opening` (plain in edit mode, the client pair in Preview — the Masthead opening's rule). **The Share rail** is INFERRED again (user call, 11 Sep 2026: "the Run could get missed or mixed up"): `_icon_preprocess_field_work_canvas()` regroups the tree the naive formatter hands over — consecutive Content, Results, Video hosted and Video embed blocks under one rail (`field--node--field-work-canvas.html.twig` draws the chunk), a Gallery, a scroller, a quote or the masthead breaking the run — the Paragraphs template's rule, so an editor only stacks blocks. The **Run** component is hidden from the library (`canvas-library.php`) and `scripts/work-runs-unwrap.php` lifted every article's Run children to the top of its tree; a Run still renders where one remains. In the editor Canvas renders the tree flat, so a text block shows a "Share rail" marker in the gutter (`utilities/work-article.css`, `html[data-icon-editing]`); the page and Preview draw the real rail. **A starter:** the **Case study** pattern (`canvas.pattern.case_study`, `scripts/case-study-pattern.php`; the library's Patterns tab) drops the challenge, an empty Gallery row, what we made, a Results band with two results and a closing text block; the masthead comes on the article's first save. **Migration**: `scripts/work-canvas.php` builds each article's tree from its Paragraphs body (prose → Content on `canvas_html_block`, gallery rows → Gallery + Figure, stats → Results + Result, film → Video hosted, scroller, quote), validated then saved as a new revision; the paragraphs stay (hidden from the form and the full display) so emptying the Canvas field undoes it. **Canvas is THE editor of a Work node (user call, 11 Sep 2026):** the Edit tab — and so the Navigation bar's Edit button and the content list's Edit — opens `/canvas/editor/node/{nid}` (`icon_site.node_canvas`, `icon_site_menu_local_tasks_alter()`, `icon_site_entity_operation_alter()`); the classic form is the **Fields** tab behind it and shows a Build in Canvas button where the Body was. **The frame IS a component (user call, 11 Sep 2026: "why don't we get the masthead to work like this"):** `sdc.icon.work-masthead` (Work article folder) stands first in every article's tree — Project name, Client, Feature statement, Categories (a list of the six keys), Deliverables (a list), Banner picture or Banner film, Page colour, Ink colour — and is edited by clicking it, like any block. Its values are the node fields' MIRROR: `icon_site_node_presave()` copies them into the fields the cards, listings and filters read on every save (a Canvas publish included), and adds the component from the fields to an article whose tree has none (a new article from the classic form); the classic Fields form writes the other way through an entity builder (`_icon_site_work_masthead_from_fields()`). The Page data panel hides those eight fields; the theme's own frame (`includes/work-frame.html.twig`) stands in only on an article without the component, on the page and in the editor's shell. `<html>`'s colours are read off the component (the draft's, in the editor) with the fields as fallback (`_icon_work_masthead()`). `scripts/work-masthead.php` gave every article its masthead (a save each; presave did the rest). The hover-chip bridge from earlier the same day is gone: Canvas's own selection does its job. The Page data panel itself is dressed by `icon_site/css/page-data.css` (labels, help, sections under hairlines, category pills, the colour pair with swatches at the top, the deliverables list on Canvas's own tabledrag — the Drupal default; the panel sorter was tried on it and taken off, user call). Note: Canvas 1.11's editor page builds its own response, so a library attached by `hook_page_attachments()` never reaches it — the form attaches `icon_site/page_data` itself. **The gate**: Canvas 1.11 refuses a Canvas field on anything but its own Page ("for now", [#3498525](https://www.drupal.org/i/3498525)); `patches/canvas-node-canvas-field.patch` (composer-patches) lifts the five-line check in `ComponentTreeLoader::getCanvasFieldName()` — the path Canvas's own article tests exercise. Drop the patch when the issue lands. **Figure** grew `film` + `film_layer` props: Canvas's image prop cannot carry a film, and the layered figures (a reel under a logo, a reel over a still) needed one.
- **Canvas 1.11 (11 Sep 2026).** Page variants replace theme page regions; the site never used regions, so nothing migrated. The "Page content" marker component is hidden from the library (`canvas-library.php`). Content templates (`node.<bundle>.<view mode>` config with field-bound props and one exposed slot) exist but still need the same node Canvas field, so the Work article went the field way.
- **The Masthead's slots and the gutter (11 Sep 2026; user call: "no double up").** The masthead pads on the page gutter for its centred column. A block placed **Under the heading** sits in that column and drops its own gutter (`.page-header__inner .pull-quote / .work-stats / .columns / .content-page`); the **Full-width band** is wrapped in `.page-header__band`, which gives the gutter back (a negative margin), so what is placed there pads once on its own — Columns, a pull quote — and the Filmstrip bleeds as it does anywhere.
- **Columns (11 Sep 2026).** The `columns` SDC (the Page folder then; the Layout folder since the 14 Sep reshape) is a LAYOUT component the way Canvas's own Columns example is: `columns` 2 / 3 / 4, `gap` normal / tight, and a drop slot per column (column_1…column_4) that takes any block — not a text field per column. The twig renders only as many slots as the count says (a filled slot beyond it is kept and returns when the count grows); `src/components/columns.css` puts them on the page grid — stacked, two from 768, the count from 1024 — self-padded on the page gutter, and strips a held block's own framing (the prose measure and gutter, the pull quote's and results band's gutter) the way the article frames and the masthead do.
- **Columns in a box (14 Sep 2026; user ask: "make them inside a box (add some padding, and our corners) with the ability to change background and foreground colours").** `box` none / white / light-grey / black / blue / custom (+ `bg`, `ink` hex): `.columns--box` pads the columns (`--box-pad`) on the brand corner (`--radius-icon`), standing on the page gutter as a margin instead of padding on it, on one of the grounds a page can open on. The theme class rides on the box itself — `dark`, `theme-blue`, or `theme-work-article` with `--work-article-bg` / `--work-article-text` as the only inline style (white, light grey, or the validated custom pair; the twig falls back to light grey and black on a bad hex) — and the box re-declares the registered `--color-*` aliases (background, foreground, border, icon-black/grey/blue), which resolve where they are declared (LESSONS.md), so prose and components inside take the ground's ink. A boxed Columns inside a column drops the margin and keeps its padding. The library calls it **Columns and boxes** now (user call). `gap` and `box` are required with a default, like `columns`, so the panel's selects offer only the real choices (an optional select adds Drupal's "- None -" above a "No box" that means the same thing; user catch). `columns` takes **1** as well (user ask, same day: "so we can add a single column box"): one slot at the page's width, `.columns--1` holding the single track from 768px where the grid would pair. **Link** (15 Sep; user ask: "an optional link to the full box … a hover state, inline with other parts of the site"): `link` + `link_label` — a stretched anchor (`.columns__link`) under the columns, which rise above it with pointer events off except on what they hold to click, so the box goes somewhere without nesting the links inside; on hover the ground steps 6% toward its ink and the headings and links inside go ICON blue, pointer devices only.
- **Expertise links (15 Sep 2026; user ask: "Can you make this a component I can use on the Expertise landing page?").** The intro's "Our expertise" list is its own component: `src/components/expertise-links.css` (the row, the sticky label at 50vh, the list, the inked `.is-active`, the no-JS first item) and `js/expertise-links.js` (the scroll highlight, per instance, one listener) lifted out of `utilities/home-c.css` / `js/home-c.js`, which keep only the band's grid placement of `.intro__expertise`. Drupal: the `expertise-links` SDC — **Expertise links (group)**, `label`, slot `links` — and `intro-expertise` renamed **Expertise link (item)**, both in a new **Expertise** folder (`scripts/canvas-library.php`); the Intro's slot takes the same item, and `intro.twig` renders the same row with the component's classes (`libraryOverrides` attaches `icon/expertise-links` beside `icon/home-c`). On a page the row pads on the page gutter (`--page`), which Columns and boxes strips. In the editor the empty slot's placeholder is sized to a link-list (`.expertise-links__list > .canvas--slot-empty-placeholder`): a hairline-thin slot lost every drop to the column around it (user report, 15 Sep: "I cant add expertise items to the group?"; the Filmstrip's fix).
- **The Style menu shows what fits (15 Sep 2026; user question: "can the styles only show options for that selection?").** CKEditor lists every style and greys out those that cannot apply to the block the cursor is in; `icon_site/css/editor-panel.css` (the `editor_panel` library the component instance form attaches) hides the greyed tiles, so a paragraph offers its paragraph styles and a heading its variants. Our CSS over CKEditor's UI — CKEditor has no such setting — and the trade the user chose: in a paragraph the heading styles are not on view until the block is a heading. One rule; delete it for the full list. The fact card's uppercase title takes the caps line-height of 1, like every other uppercase heading, and its label the statement's 1.2.
- **AI alt text waits for a key (16 Sep 2026; user report: a warning and two "Oops" errors on a video upload).** `ai_image_alt_text` went in on 8 Sep with the AI modules and was set to auto-generate on every image upload, with no provider configured: it warned on every media form and its post-upload call failed, taking the media library's upload validation down with it (`FileUploadForm::validateUploadElement`, `fids` missing on the rebuild). `autogenerate` is off in `ai_image_alt_text.settings`; the "Generate alt text" button stays on image uploads and works once an OpenAI key is set on the host (the provider module is installed). The Case study pattern's file also re-keys its Picture child to Canvas's own `'1:column_1:0:<uuid>'` form on this export — a normalisation, no change of content.
- **The page move is a wipe (16 Sep 2026; user ask, after leoparpeix.com: "the blue animates from the bottom left, in the centre is our Make what matters svg … solid white. Then the blue shrinks back (top left corner) to the new page and everything animates in as it does").** Still one cross-document View Transition (`@view-transition { navigation: auto }`), still CSS: `js/page-transition.js` plants a `.page-wipe` square plus `.page-wipe__mark` (the lockup from `includes/lockup.html.twig`, copied into the script) in the new page at `pagereveal`, named elements last in the body (nothing at `pageswap`: an element planted in the leaving page painted once live before the snapshot — a split-second flash of blue, user catch — so the new page's snapshot plays both halves); `utilities/page-transition.css` grows the new snapshot — a disc wider than the viewport's diagonal, centred (user call after watching leoparpeix.com's disc: a circle from the centre; it was a square from the bottom-left corner first) — over the old page's frozen frame (600ms, standard in-out ease; user call: the reference's pace), clips the mark's snapshot to a circle on the disc's own timings and ease so it only exists inside the blue (user call: masked, the intro and outro the same), shrinks the disc back to the centre (500ms from 850ms; the wordmark and pill stay above the blue) and steps the pages under it at 600ms; `--animate-hold` is 1.35s. The homepage's blue loader still lights as already landed (`ICON.pageEntering`). The ground note and the fade-through type are gone. Reduced motion: off; Firefox: a plain navigation.
- **The strips lay out flat in the editor (16 Sep 2026; user ask: "How many images are there in this scroller? I can see 3, I cant see the rest, so I cant edit or change the order").** Inside Canvas's frame js/work-scroller.js and js/filmstrip.js mark their root `.is-editing` (instead of running the drift) and the css wraps the track into a grid of small, level cards — every Picture, photo and fact card in view to select and drag into order; the live page keeps the drift and the tilts. The Layers panel lists the same children for reordering.
- **One Picture everywhere (15 Sep 2026; user call: "a single image/video, foreground/background, optional colour, square, landscape, portrait — and I can use the columns for layout").** The `news-article-figure` SDC — **Picture**, Pictures folder — is the one picture component: `picture` and `float` (the site's picture-or-film picks), `shape` natural / landscape 16:9 / square / portrait 7:10, `ground` (hex) and `pad` for a layered figure; a cut-out sets it layered, a layered Natural takes landscape, and an empty first pick leaves the colour ground (`src/components/picture.css`; the layered anatomy is the banner's, `utilities/work-article.css`, keyed on `.picture--layered` too, and the article's lean and gutter strip cover `.picture` and `.columns`). Layout is **Columns and boxes** on the Tight gap — the gallery's 16px. **Parallax** (16 Sep; user ask: "add a tick box to deselect parallax, there may be a case where we need to see the full image"): a boolean prop, on by default and on for every Picture placed before; `.picture--parallax` drifts the media ±2% on a named view-timeline, zoomed 6% on `transform` alone (the reveal owns clip-path, scale and opacity), so off it shows whole. The Work article's lean covers `.picture` (work-article.css) and the News article's does too (news-article.css, `--nl-skew`); the reveal is `.media-reveal` on every Picture. **Picture scroller (group)** (`work-scroller`) holds Pictures in its `pictures` slot with its ground colour; a `.picture` in the track is a card (`work-scroller.css` scopes the sizing; js/work-scroller.js counts cards, not children, and holds still in the editor's frame). **Gallery row** and **Gallery row picture** are hidden from the library, kept for the articles still composed from paragraphs (their templates honour the old inputs, as does Picture's `image`). `scripts/pictures.php` (`ICON_SEED=1`) turned every placed row into a Columns of Pictures and every scroller into the group form (its `films` only ever held the schema's example); `scripts/work-canvas.php` seeds the same shapes from paragraphs.
- **Gallery row picture and Work masthead take the one pick (15 Sep 2026; user catch: "I thought we had a global rule for Add Image/Video").** The two components still on Canvas's own image + video fields moved to the site's `x-icon-media: picture-or-film` shape — the one Add media, picture or film, with upload, library, folders and search: Gallery row picture's `media` + `film` + `film_layer` became **Picture** (the whole of a plain figure; the ground of a layered one) and **Cut-out** (what floats over it), and Work masthead's `banner` + `banner_film` became **Banner**. The twigs resolve a media id through `icon_media(id, 'work_media')` and still honour the old inputs on an instance placed before; the theme's paragraph path and `scripts/work-canvas.php` feed the new props; `icon_site_node_presave()` mirrors `banner` (reading `banner_film` from an older instance). `scripts/single-picks.php` (`ICON_SEED=1`) rewrites the placed figures and mastheads onto the current versions — saved pages and articles, drafts untouched. Picture scroller is the one component left on Canvas's image list: its many pictures want a row of items, a later slice. A Canvas component's config carries every version its placed instances point at: on the host, a component whose instances sit on a version the imported config lacks throws on render (`OutOfRangeException: The requested version … is not available`) — every page holding one answers 500 — so a migration that moves instances onto the active version must ship with the config that has that version (the first attempt here committed a stale export, see LESSONS.md), and a repoint to the active version is the recovery.
- **One style per element (15 Sep 2026; user catch: `<p class="is-large is-small">`).** CKEditor's Style menu stacks block styles; `icon_site_exclusive_styles` (`icon_site.ckeditor5.yml`, `js/ckeditor5/exclusive-styles.js`, a plain script on core's DLL that Drupal resolves from `window.CKEditor5.iconExclusiveStyles`) rides along wherever the Style plugin is on and, after a style is applied, toggles off every other style defined for the same element on the selected blocks — the plugin's own toggle, run in reverse. Back to the default is CKEditor's own gesture: click the highlighted style again (user question: "no way of selecting the default size?"). The style labels are short now — Large, Small, Caps, Sentence case, Miller (serif), Caps label, Slash bullets, Caps list — since the menu shows only the block's own styles and the panel truncated "Paragraph — …" to nothing; the tiles' names wrap (editor-panel.css).
- **The Content toolbar wraps (15 Sep 2026; user question: should the text editor be an overlay?).** No overlay: Canvas edits every prop in its right-hand panel through the normal field widget, and there is no in-place editing yet — the panel is the editor, and it drags wider. What hid the options was CKEditor 5 grouping a too-wide toolbar into ⋮, so `editor.editor.canvas_html_block` (the format Canvas gives a block-level HTML prop) now carries a wrapping breakpoint (`-` after the link button: core's `shouldNotGroupWhenFull`) — heading, bold, italic, link on the first row; lists, the Style menu (the prose voice's own classes: Paragraph — Large / Small / Caps, the heading variants, the slash and caps lists), blockquote and source on the second. Alignment and inline code are gone: neither is in the prose voice. The format's `allowed_html` is regenerated from the plugins (so `<u>`, `<s>`, `<code>`, `<hr>` and the table tags, which no plugin provided, are no longer allowed), and the Source Editing list drops the tags plugins already cover — that config as Canvas shipped it failed its own validation on eight counts, which would have stopped the host's import the first time it changed.
- **The News and Work landings are Canvas pages (11 Sep 2026; user call: edit their headings and theme like About).** `/news` and `/work` are canvas_pages (3 and 4, by alias; `scripts/landing-pages.php` builds them), each a **Masthead** (accent, headline, Opens on — both open dark) with the **Subscribe bar (masthead)** component (`sdc.icon.subscribe-bar`, the old include as an SDC, Parts folder then; Mastheads since the 14 Sep reshape) in its Under-the-heading slot, over the **News listing** / **Work listing** block — the View's new `landing` block display (`block.views_block.news-landing`, `-landing`; the `page_1` page displays and their routes are gone). The listing block's template (`views-view--news--landing.html.twig`, `--work--landing`) is the old page template minus the masthead: the chips in a standalone **listing band** (`src/components/listing-band.css` — the masthead band's recipe on its own; `--open` drops the bottom rule on News), the rows, the pager, the status line; `icon_preprocess_views_view()` keys on `landing` and the chips link to the page they sit on (`_icon_listing_url()`, `<current>` → the alias), lazy loading and the in-place filters ride along unchanged. The dark opening: the Masthead's Opens on puts `dark` on `<html>`; on News the first story's `data-theme-handover="end"` still decides the handover — `js/theme-handover.js` prefers an explicit "end" carrier over the Masthead's. The news article's category link and the work article's category chips point at the aliases (`Url::fromUserInput`). What editors gain: the words, the colour, and any other block above or below the listing.
- **A Canvas page opens on what its Masthead says (11 Sep 2026).** The Masthead's `handover` boolean became **Opens on** (`opening`: none / dark / blue / light-grey / custom, with `bg` + `ink` hex props for custom). `icon_preprocess_html()` reads it off the page's tree (`_icon_canvas_page_opening()`) and puts the class on `<html>` — `dark`, `theme-blue`, or the work article's `theme-work-article` chameleon with the pair inline (light grey is `var(--light-grey)` + `var(--black)`) — and the masthead names the same class in `data-theme-handover-class` for `js/theme-handover.js`. The third ICON colour, light grey `#edeef5`, is the fixed `--light-grey` / `--color-light-grey`. The Views listings pass `opening` in their embeds (work: dark). **The editor's preview matches the page** (user call): `_icon_route_canvas_page()` finds the Canvas page on its own route AND on the `canvas.api.layout.*` preview routes — there it is the auto-save DRAFT the editor is showing — so `page-canvas` (the block rhythm) / `page-home` land in the preview too. The opening COLOUR shows in Preview mode only (user call): in the editor the theme sets `data-icon-opening` instead of the class and `js-theme/opening.js` (`icon/opening`, in the head) applies it unless the frame is the editor's own (`data-canvas-preview` on the iframe), where it marks the root `data-icon-opening-off` and `js/theme-handover.js` stands down.
- **The Team profiles panel is the people (11 Sep 2026; user call: "how do I edit the profiles?").** The block's Canvas panel lists every Team member the editor may see, published or not (an unpublished one dimmed, "not in the grid"), in grid order: drag to reorder posts the ids to `/icon-site/team-action?op=order` (`TeamActionController`, CSRF-protected, `edit any team_member content`) which writes each member's Order field at once, then reloads — the marquee's pattern, and `js/panel-sortable.js` now posts for ANY card carrying `data-order-url`; **Edit** opens the member's node form in a dialog over the editor (`_icon_site_panel_node_form_alter()` dialogs `team_member` as it does `hero_slide`, the Order field hidden since the drag owns it); **+ Add member** the add form the same way. The one setting stays the heading.
- **The clients marquee is its own behaviour, on the shared strip engine (11 Sep 2026).** The `clients` SDC attached `icon/home-c` — the whole homepage bundle, GSAP and Lenis — so the About page's marquee brought Lenis smooth scroll, which took every wheel event and scrolled the page behind the open team panel. `js/clients-marquee.js` (`icon/clients-marquee`) is the marquee now, on `js/strip-drift.js` (`icon/strip-drift`: the drift / drag / momentum / re-pointing fling integrator, lifted at its third consumer — the gallery scroller and the filmstrip ride it too), and home-c.js starts Lenis on `.page-home` only.
- **The Contact page (11 Sep 2026; user ask, design: the live iconagency.com.au/contact, dressed in the system's own voices).** `templates/contact.html`, opening DARK and staying there (no handover; the footer is the same ground): the `page-header--stack` masthead (accent "Contact" over "This is how / we finally meet", no Subscribe bar, no filter band), then `.contact-page` (`src/utilities/contact-page.css`) — the enquiry form on the left (`.contact-form`, `src/components/contact-form.css`: the lead in the mid voice at medium, the intro a Paragraph, each field its label in the Paragraph voice at semibold over THE hairline with the answer typed beneath in the same voice, focus filling the hairline; a paperclip file input on the message, a captcha slot, "Send your message" as the quiet invitation link) and the four offices OPEN on the right (`.office-list`, `src/components/office-list.css`: the footer accordion's open row exactly — icon, the city in the mid voice with its place name muted beside it, drawn hairlines, the address then Phone then email in one column under the name). Both columns start on a drawn hairline with the office row's air beneath it, so the lead and "Melbourne" sit level (user annotation). The Paragraph voice is the new `--voice-paragraph` token (typography.css), which base/prose.css now reads too. The footer wears `.site-footer--contact`: its CTA row is "Join our newsletter" in the display caps with the Subscribe bar centred beneath (`.site-footer__cta--newsletter`, in the markup) instead of LET'S TALK / Get in touch, which would invite the reader to the page they are on, and the body — the newsletter column and the office accordion, whose details are open just above — is hidden (user calls). Drupal: a Canvas page — Masthead over a Columns of the contact **Webform** block (these classes are its visual spec; the captcha is the Webform's own) and an `office-list` SDC over the Offices content (Content → Offices); the footer modifier is set by path in page.html.twig.
- **Carrying a page's content to the host (11 Sep 2026; user ask: "update the About page CMS on Upsun to match our local").** `scripts/page-sync.php`: `ICON_SYNC=export ICON_ALIAS=/about` writes the page's tree as JSON with every media and node reference as a UUID; on the host, `ICON_SEED=1 ICON_SYNC=import ICON_ALIAS=/about ICON_FILE=…` looks each UUID up on that side, takes that side's component versions, saves as a new published revision as user 1 and drops the open draft. A reference the host does not have — a file uploaded locally, since CMS files never travel — drops that component and its children and is reported (the About sync dropped one Photo, the jo-chris film). Copy the two files up with `upsun ssh … 'cat > /tmp/x' < x`; the script runs from /tmp, so it needs no deploy.
- **The primary navigation is the Main navigation menu (14 Sep 2026; user ask: "Where do I manage this?").** Structure → Menus → Main navigation. `icon_site_primary_nav()` reads it two levels deep (access-checked, menu order, the profile's Home link skipped — the pill's glyph is home) and `icon_preprocess_page()` hands it to `includes/site-header.html.twig` as `nav`: the pill prints the top level in caps, the FIRST link with children opens the drawer of them (one drawer, as designed — Expertise), and the mobile menu prints the same two lists (children in caps). `scripts/main-menu.php` seeds the links the template used to hard-code, once: Work, Expertise (Interdisciplinary, Creative, Communications, Digital, Reputation, Behaviour change, Production — all on `/services`, the sections as fragments), About, News, Contact. Still static: `aria-current` on the active item.
- **The library is eleven sections, a strip with its items (14 Sep 2026; user call: "grouped as its own section, that way a user will know they are connected … consistent naming, strip and item").** `scripts/canvas-library.php`, in the order a page is built: **Mastheads** (Masthead, Masthead subscribe bar (item), Work masthead), **Layout** (Columns, Line, with room for more), **Text** (Content, Pull quote), **Results** (Result stats (group), Result stats number (item)) — the numbers are copy, so they sit with it (user call) — **Pictures** (Picture, Picture scroller), **Gallery** (Gallery row (group), Gallery row picture (item)), **Filmstrip** (Filmstrip (group), Filmstrip fact card (item), Filmstrip photo (item) — the intro's cards too; the library lists a section alphabetically, so an item's name starts with its strip's to sit under it), **Video**, **ICON** (Clients scroller, Contact form, Offices: one — one office chosen in the panel, for composing with Columns — Team profiles; Offices: all is off the library, the contact page being rebuilt from Offices: one, and its block stays until then), **Listings** (News: latest / listing, Work: latest / listing — subject first, so a subject's pair sit together; the two Next up rails are off the library, since the article templates place them), **Homepage** (Featured work, Homepage hero, Intro (group), Intro expertise link (item)); every item's name starts with its strip's, so the group lists first and its items under it. The "Parts" folder is gone. Names are the SDCs' `name:`, groups their `group:`; the panel notes on the strip-holding components name the sections. Also: an EMPTY strip in the editor now stands a card's size (`.filmstrip__track > .canvas--slot-empty-placeholder`) and the drift engine leaves it alone (`filmstrip.js` counts cards, not children), so the first Photo has something to drop onto — a card dropped "into" a moving 32px sliver landed beside the strip instead (user report).
- **The Venn diagram (14 Sep 2026; user ask: "create this Venn as HTML/CSS and animate — the draw animation from Make what matters").** `venn` SDC (ICON section; five words: the centre, which takes a line break, and the four practices) rendering `src/components/venn.css`'s inline SVG on a 440 viewBox: four hairline rings on a cross, SVG-pattern hatches in the four lenses (dots, diagonals, cross-hatch, diagonals the other way — the lens paths are the two-circle intersections, computed once), the blue core. It draws on the shared `[data-animate]` host (`icon/reveal`): rings on `stroke-dashoffset` from twelve o'clock a beat apart, hatches surface, core grows, words rise; reduced motion and no JS see it whole. The dash is the circumference in viewBox units, so the stroke must scale with the box — `vector-effect: non-scaling-stroke` left a gap at the top of every ring. The pattern ids carry a per-instance id so two on a page keep their own hatches. The design-system index now runs the reveal host (js-animations + js/reveal.js) so its demos animate, with a "Draw it again" control on this one.
- **A picture or a film is ONE pick (14 Sep 2026; user call: "a single Add, and on the overlay choose the tab, upload, filter — make consistent").** A prop `type: integer` with `x-icon-media: picture-or-film` is stored as a reference to an Image or Video media item and edited with the media library widget — one Add media, Image and Video tabs, upload and filters, the same dialog as everywhere else (`icon_site_canvas_storable_prop_shape_alter()`, beside the Icon mapping). The prop evaluates to the media ID; the component hands it to the Twig function `icon_media(id, style)` (IconSiteExtension) for `{type, src, alt, width, height}` — an Image through the style, a Video as its file with its poster. Filmstrip photo (item) is the first to use it (`media`, replacing `image` + `film`; the template still honours the old pair on an instance placed before); `scripts/photo-media.php` (idempotent, `ICON_SEED=1`) rewrites saved pages and articles onto it — run on local and the host; open drafts keep the old pair and still render. Still two-field: the Work masthead's banner + banner film (mirrored into node fields), Gallery row picture (two pictures + film + layer) and Picture scroller (pictures + films) — the same shape applies, each a separate slice.
- **Every film is a media pick (14 Sep 2026; user call: "look at our other image and video selectors").** Video hosted's film was a file path (`video_src`, a link field) while every other film — the masthead banner, the gallery picture, the filmstrip photo, the picture scroller — is a Video from the media library; it is `film` (`$ref video`) now, the same picker everywhere. An instance placed before still renders (the template honours `video_src`); `scripts/work-video-film.php` (idempotent, `ICON_SEED=1`) rewrites those to the media whose file is that path, onto the current version — run on local and the host. The only URL field left on a media component is Video embed's, which is the YouTube/Vimeo address by design.
- **The Contact page in Drupal (11 Sep 2026).** A Canvas page at `/contact` (`scripts/contact-page.php`, idempotent, `ICON_SEED=1`): the Masthead with the new opening **"Black, all the way down (no handover)"** (`opening: dark-hold` — `_icon_canvas_page_opening()` maps it to `dark` and page-header.twig does not mark it for the handover; the caps prop takes a line break, printed through `nl2br`), over a two-column **Columns** holding the **Contact form** block (`icon_contact_form`, Listings folder then; ICON since the 14 Sep reshape) and the **Offices** block (`icon_office_list`, Listings). The form is the core **Contact** module's "Contact" form (`contact.form.contact`, the default; Content → Contact forms sets the recipients — melbourne@iconagency.com.au to start — the reply and the message shown; anonymous and authenticated may use it): four fields on the message (`field_contact_first_name`, `field_contact_last_name`, `field_contact_phone` (telephone), `field_contact_attachment` (file, 10 MB, documents and images)) plus core's email and message; the sender's name and the subject are not asked — an entity builder writes them from the names (`_icon_site_contact_message_builder()`); Preview and "send yourself a copy" are off. `icon_site_form_contact_message_contact_form_alter()` dresses it in the template's classes (label over hairline, the quiet-link Send) and the block builds it into the `contact-form` SDC's slot as rendered markup — a slot must be a render array all the way down (`Element::isRenderArray()`), which a built form is not. The block's lead and intro are its settings (`block.settings.icon_contact_form` schema, FullyValidatable, as Canvas requires). The Offices block feeds `icon_site_offices()` to the `office-list` SDC (props the block's; Canvas does not register the SDC — its `icon` prop is integer-or-null — so it is not on the library and needs no config). The footer's contact dress (`.site-footer--contact`: the newsletter CTA, no body) is `footer_contact` from `icon_preprocess_page()`, keyed on the page's alias `/contact` (`_icon_is_contact_page()`, in the editor's preview too). Not yet: a captcha (the captcha module) and outbound mail on the host.
- **The strip is ONE system — the Filmstrip component and its cards (11 Sep 2026; user call: "The Stat cards should work like the homepage intro. I feel like these are two systems").** The `filmstrip` SDC (Pictures folder; a Cards slot) with `intro-photo` (Photo — a picture, or a Film that loops muted) and `intro-fact` (Fact card — icon, heading, label) components placed in it, exactly as the homepage intro's strip has always been: click a card to edit it in the panel, add one from the library, drag it in Layers. The **Filmstrip block** (`block.icon_filmstrip`, icon_site) that bundled photos, films and fact cards into one panel form — a reel of Intro-tagged media with Add to strip and an Available list, six fact rows, "a fact card after every N" — is OFF the library; `scripts/filmstrip-components.php` (idempotent; `ICON_SEED=1`, run on the host after the deploy) turns every placed block into the component and its cards in the order the block drew them, keeping the block's uuid, and drops the page's open draft. `scripts/about-page.php` places the component form. The block plugin can be deleted once every environment has run the script.
- **The filmstrip is a component of its own (10 Sep 2026).** The `filmstrip` SDC (ICON folder; one optional prop, the accessible label; a **Cards** slot of `intro-photo` / `intro-fact` children) is the intro's strip on any page — each placement holds its own cards, a new card kind is a new child component. `js/filmstrip.js` (`icon/filmstrip`, attached by the SDC) is the gallery scroller's vanilla engine with the lean written as one `--strip-lean` for the CSS to turn the cards by; the GSAP strip left home-c.js. The intro embeds the filmstrip and hands its `strip` slot through, so the homepage tree and `scripts/home-page.php` are unchanged.
- **The intro is props + SLOTS.** `intro` takes the words as props (masthead accent + caps, the intro text, the About link and label, the expertise heading) and exposes two slots: **Filmstrip cards** (`strip`), filled with `intro-photo` (a media-library image; alt from the media item) and `intro-fact` (icon from an enum of the four brand icons in `templates/includes/fact-icon.html.twig`, heading, label) components in whatever order they should drift; and **Expertise links** (`expertise`), filled with `intro-expertise` (label + link) components. Repeatable content in Canvas is child components in a slot, not array props — that is the pattern for every list an editor owns. `clients` (its accessible label) is an SDC over the theme's logos; a `client` media type (per `assets/client-logos/README.md`) is the later slice that makes the marquee editable. The fact card's `icon` prop is `type: integer` with `x-icon-media: true`, which icon_site's `hook_canvas_storable_prop_shape_alter()` maps to a reference to **Icon** media (`media.type.icon`: file source, `svg`, an Order field) edited as a SELECT — so the dropdown is the Fact icons list at `/admin/content/fact-icons` (add, drag to reorder, rename, replace, delete; the same list page class as Client logos, `MediaListFormBase`). The prop evaluates to the media ID — Canvas's widgets display a prop's RESOLVED value, and a select only preselects when that value is an option key — and `icon_inline_svg()` loads the media's file and inlines it (scripts and handlers stripped, class + aria-hidden added) so `fill="currentColor"` takes the card's white. Canvas has no SVG media shape of its own — the Image shape only matches Image-source media, and Drupal's image field refuses SVG — hence the mapping. Replacing an icon's file may need a cache rebuild to show on already-rendered pages.
- **The Icon list holds the live site's icons.** `scripts/fact-icons.php` (`ICON_SEED=1`) adds every unique SVG harvested from iconagency.com.au — minus the chrome (arrows, socials, search, plus, share, logo, chat and phone glyphs) and the eight already seeded — as Icon media, idempotent by name, weighted after the existing ones: 11 of ICON's own line icons (the masthead marks on practice and landing pages: Aperture, Cube, Easel, Flag, Megaphone, Paper plane, Robot, Smiley, Speech bubble, Spinning top, Star burst) and 36 Font Awesome Pro service-card icons (Regular weight — the licence covers their reuse on the site). Each lives in `sample-content/icons/<slug>.svg`, recoloured to `currentColor` so it takes the ink or white of wherever it sits. The same script refreshes **Flame**'s file in place: the seeded one was the heavier 52-grid asset; the live site's flame is the lighter 278-grid drawing that matches Peace sign, World and the rest, and the design-system asset and `templates/homeC.html` now carry that drawing too.
- **The hero is an SDC in the library, and the reel is built from the pile** — `hero.component.yml` declares `libraryOverrides.dependencies` on `icon/hero-loader` (footer, weighted ahead of the externals) and `icon/home-c` (GSAP, SplitText, DrawSVG, Lenis), so the whole homepage system attaches whenever the hero renders. The template writes every piece twice (a pile card and a reel slide, in different containers); a slot child renders once, so the SDC renders the slides into `.hero__stage` as cards and `js/home-c.js` clones the reel from them when it finds `.hero__show-inner` empty — the template's own markup never takes that branch. Cards carry `data-client` / `data-client-url`, and the reel's client name renders as `<a class="hero__client-link">` when a URL is present. `work-gallery` / `work-stats` / `work-scroller` stay out of the library (arrays of objects; Paragraphs-fed).
- **The shell**: `icon_preprocess_page()` adds `.page-home` to `<main>` on the front page; the html stays light. Canvas renders the page's tree into `page.content`, so the theme's header, footer and `page.html.twig` wrap it unchanged.
- **Children in code**: a slot child is a tree item with `parent_uuid` = the parent's uuid and `slot` = the slot name (`scripts/home-page.php` builds the strip and the expertise list that way).
- **Block inputs in code**: a Views block instance must store `label`, `label_display` (`'0'`, a string) plus the plugin's defaults (`views_label`, `items_per_page`) or validation fails on `label_display`.
- **Theme assets inside an SDC**: an SDC renders with no page context (no `base_path`, no `directory`), so a component that references theme assets resolves its own path with `file_url(active_theme_path())` — `intro.twig` and `clients.twig` do; a page template keeps `base_path ~ directory`.

## Asset conventions

Client logo SVGs follow one upload rule — the component owns colour and
sizing, the file owns clean geometry — documented in
`assets/client-logos/README.md` beside the files themselves. The same rule
becomes the `client` media type's editor guidance at port time.

## Related docs
- `drupal-mapping-pattern.md` — the three-piece SDC pattern with a worked example.
- `field-naming.md` — BEM-prefixed field machine names.
- `wysiwyg-output.md` — how CKEditor body fields are styled by the `.content-page` prose wrapper.
- `tailwind-conventions.md` — the utility-vs-BEM decision and why CVA is rejected for this theme.

## Prototype paths that must revert on port

The templates are browsable as a static site on GitHub Pages, where the project
is served from a subdirectory (`/icon/`), not a domain root. Two consequences,
both deliberate, both to be undone when the markup becomes Twig:

- **The four "home" controls point at the design-system index, not `/`.** The
  header wordmark (`.site-logo`), the nav pill's home glyph
  (`.site-nav__home`), the mobile home button (`.site-header__mobile-home
  .icon-button`) and the logo inside the open mobile menu
  (`.mobile-menu__logo`) all use `href="../index.html"`. On Pages an `href="/"`
  leaves the project entirely and lands on the org root. In Drupal all four are
  the site front page: `href="/"` (or `{{ path('<front>') }}`). Each template
  carries a comment saying so above the wordmark.

- **Content links keep their real Drupal paths and do NOT resolve statically.**
  `/work`, `/news`, `/news/article`, `/news?category=…`, `/contact` and the
  rest are the routes the built site will have, so they are written as such and
  simply 404 on the preview server. Do not "fix" these to relative paths — they
  are correct, and the filter chips in particular rely on the query string
  being honoured server-side by a Views exposed filter.
- **A news article's image figure is the image itself** (user call, Sep 2026): `.news-article__figure--natural` — on the news templates' image figures and the theme's `news-article-figure` SDC — takes the image's own height, at its own size centred (never wider than the column), with the prose's fade-up and nothing else: no 16/7 band crop, no `.media-reveal` mask, no parallax drift. The film figure and the work article's figures keep the band. In `src/utilities/news-article.css`, after the band and parallax rules, so it wins by order.
- **The /news and /work listings lazy-load.** Each View still pages server-side (12 a page, the `full` pager in the markup for no-JS and crawlers); `js-theme/listing-lazy.js` (`icon/listing-lazy`, attached by both listings' view preprocess; the status noun is the pager's own) hides the pager, fetches the pager's own Next link as the reader nears the foot of the list, lifts the new `.news-list__item` rows in with the listing's fade-up stagger and the card tilt, and keeps a status line ("12 of 282 stories" → "That is all 282 stories.") with a three-dot loading mark (`src/listing-lazy.css`). A crossing the observer never sees (a flick past the foot) is caught by a scroll check. The category chips NAVIGATE on both listings — over a paged archive only the server-side filter is honest — so the in-place filters (`js/news.js`, `js/work-filter.js`) are stepped around by a capture-phase click listener; a filtered page lazy-loads the same way. A work row's caption, figure and card tilt are re-armed on arrival.
- **A Work item can wear the old site's layered tile** (user call, Sep 2026, for folio items brought over from iconagency.com.au): the Work form has a closed **Legacy folio card (old site)** details holding `field_work_legacy_layers` — one to six square cut-out images, back to front, via the media library — and when any are set the work card renders `.work__figure--legacy` (`work-card` SDC `layers` prop, from `_icon_preprocess_node_work()`) instead of the Tile: a stack in perspective that tilts to the cursor further than the frame does (the same `--mx`/`--my` the frame reads, so no script) and lifts every layer a step further in Z on hover — the old site's 3D illusion, `src/utilities/home-c.css`. The design system's `work-landing-b.html` shows it on The Athlete's Foot tile (`assets/work/legacy/taf-layer-*.png`); `scripts/work-legacy-content.php` seeds the same five layers on `/work/fit-for-every-run`. The field is kept off the article's own view displays.
- **Environments are layered, and dev-only config is split out**: `settings.php` loads, in order, `settings.platform.php` (hosting; present when `PLATFORM_PROJECT` is set — arrives with the hosting config), DDEV's generated `settings.ddev.php`, then its own environment switch, then the gitignored `settings.local.php`. Under DDEV the switch turns on the **`local` config split** (`config_split.config_split.local`, folder `drupal/config/local`: devel and stage_file_proxy and their config, kept out of `config/sync` so they never reach production), names the **environment indicator** "Local" in green, and turns CSS/JS aggregation off. The indicator only knows the classic toolbar; on this site (the Navigation module) it falls back to an in-flow bar at the top of the body, which Navigation's fixed top bar covers while the page is still pushed down by the bar's height — a blank band under the top bar. So `icon_site_page_top()` (ordered after the module's own hook by `hook_module_implements_alter`; core has no `page_top` alter) replaces the bar with its attachments alone (the tinted favicon stays), and `Plugin\TopBarItem\EnvironmentBadge` puts the environment name, in its configured colours, in the top bar's Context region beside the page title (`css/environment-badge.css` wears Navigation's `#extra-specificity-hack` selector so its palette wins). Every other environment leaves the split off and sets its own indicator name and colour in its settings layer. `drush cex` writes the split's config to its folder on its own; `drush config:status` stays clean. stage_file_proxy's origin is set per environment in settings once the hosting exists.
- **Redis is the cache backend, and the AI modules are in** (8 Sep 2026). Contrib `redis` with DDEV's redis add-on locally (`ddev add-on get ddev/ddev-redis`: the service, `settings.ddev.redis.php` — generated, loaded by `settings.ddev.php` — which makes Redis the default cache backend, the lock, flood and cache-tag checksum services, and the container cache; `ddev redis-cli` / `ddev redis-flush`); the hosting gets the same lines in its settings layer with the Redis relationship from the platform config. Confirm with `drush ev 'print get_class(\Drupal::cache("render"));'` → `Drupal\redis\Cache\PhpRedis`. The AI layer is `ai` + `ai_provider_openai` + `ai_image_alt_text` + `key` (all in `core.extension`), configured as Frank's build is: the provider reads its key from the `openai_api_key` Key entity, whose provider is the **environment variable `OPENAI_API_KEY`** — never config, never the repo (locally: `drupal/.ddev/.env.web`, gitignored; on the host: a project variable). Until a key is present the alt-text module has no provider and adds no button (its settings page warns). `ai_image_alt_text.settings` carries Frank's prompt, the `ai_image_alt_text` image style (200px PNG) and autogenerate on; `icon_site` removes the button from SVG uploads (`icon_site_field_widget_single_element_form_alter`, ordered after the module's own alter) and says why in the field description — the forty lines lifted from Frank's `icon_ai_alt_text` module rather than a second module.
- **The quality gate** (`npm run check`, and `.github/workflows/quality.yml` on every push and pull request): PHPCS with Drupal, DrupalPractice and PHP 8.4 compatibility (`drupal/.phpcs.xml.dist`), PHPStan level 1 with the Drupal extension and deprecation rules (`drupal/phpstan.neon`), Drupal Rector dry-run (`drupal/rector.php`), ESLint on the authored JavaScript (`eslint.config.mjs` — the theme's `js/` is generated and not linted twice), Stylelint on the authored CSS (`stylelint.config.mjs`, Tailwind's at-rules and the design system's notation allowed), and the design system's own `verify`. The PHP checks run natively where PHP exists (CI, a developer with PHP) and fall back to `ddev exec` on a machine without it. Runners: `drupal/scripts/checks/{php,drupal,rector}`; `npm run check:php:fix` / `check:rector:fix` / `check:js:fix` / `check:styles:fix` apply what can be applied. The seed and mirror scripts are outside the PHP gate (gated one-off tooling). Every class takes its services by injection — block plugins through `ContainerFactoryPluginInterface`, forms and controllers through `create()`; PHPStan's Drupal rules reject `private` injected properties on anything carrying `DependencySerializationTrait`, so those are `protected readonly`.
- **Cacheability travels with the media**: `_icon_media_image()`, `_icon_media_source()` / `icon_site_media_source()` take an optional `CacheableMetadata` and record the media, its access result (contexts included), the file and the image style; every preprocess that calls them merges the collector into `$variables['#cache']` (`_icon_apply_cache()` — the theme manager bubbles it), the blocks `applyTo()` their build, and the hero reel returns its collector as `cache`. Add a media call without the collector and its dependencies are lost.
- **Access and safety, after the Drupal 11 review** (`docs/DRUPAL-11-REVIEW.md`, fixes in `docs/DRUPAL-11-REMEDIATION.md`): inline SVGs go through `enshrined/svg-sanitize` before the caller's class is applied; the media helpers (`_icon_media_image()`, `icon_site_media_source()`) and the Work body's paragraph rebuild honour entity view access, so an unpublished item renders nothing; the Canvas blocks' empty results carry their list cache tags; and every seed / mirror script is gated by `scripts/_guard.php` — it refuses on production and unless run as `ddev exec "ICON_SEED=1 drush php:script scripts/<script>.php"` — and replaces a body's paragraphs only after the new revision has saved.
- **Media has a Type** (user call, Sep 2026): `field_media_category` on Image and Video media — Intro (team & social photos), Hero slides, Work, News, Content (pages), Other; Content is the default — asked for on the upload and edit forms, kept off the media displays, and an exposed **Type** filter beside Name in the media library dialog (`views.view.media_library`, widget and table displays) and on Content → Media (`views.view.media`). Existing media were tagged by where their file lives (`home/` → intro, `work/` → work, `news/` → news, the hero slides' media → hero).
- **Every page H1 enters on the word cascade (9 Sep 2026).** The intro's `[data-reveal-words]` — the /work and /news mastheads and both article titles wear it in place of the `[data-animate]` rise; the splitter moved from home-c.js to the shared `js/reveal.js` and its CSS to `animations.css`, so it is on every page.
- **Draft, Published and Archived, on pages and both article types (10 Sep 2026).** Core's `workflows` + `content_moderation` with the stock **Editorial** workflow, applied to `canvas_page`, News and Work (`workflows.workflow.editorial`). The node forms' Published checkbox is replaced by the state select; Canvas's editor shows the same select in its page settings, because Canvas treats `moderation_state` as a stored field in its auto-save (`AutoSaveManager`). Existing content mapped itself: published stayed published, the eight unpublished sample stories became Draft. The Content editor role holds all five transitions plus `view any unpublished content` and `view latest version`. Hero slides and Offices are deliberately NOT moderated — they are structural content edited in the panels, not published pages.
- **Pages transition (9 Sep 2026).** Cross-document View Transitions, CSS-first (`src/utilities/page-transition.css`, docs/animation.md §7): a fade in every case — a crossfade when the two grounds are alike, a fade through the new ground when they differ (the script compares the old page's painted background, noted as it left, with the new page's) — with the wordmark and pill holding their place. A corner wipe and a card-to-banner morph were built and taken out again (user call). `js/page-transition.js` is a plain IIFE in `<head>` (`icon/page-transition`, `header: true`) because `pagereveal` fires before `DOMContentLoaded`; the shell render-blocks on the footer (`<link rel="expect" href="#site-footer" blocking="render">`, `id="site-footer"` on the footer include) so the incoming snapshot is the finished page. Reduced motion is an instant swap; admin pages never transition. Into the homepage the fade brings the blue loading screen in whole and the loader lights it as already landed.
- **Team profiles, on the About page (10 Sep 2026; modelled on basicagency.com/about).** A **Team member** content type (`team_member`: Name as the title, Role, Bio in `basic_html`, LinkedIn, a square Portrait as Image media, an Order field; the Content editor role holds it; no moderation — a member is published or not) and the `Team profiles` block (`block.icon_team_profiles`, icon_site, in the ICON folder; its one setting is the heading, "Leadership team") that renders every published member in Order through the `team-profiles` SDC (an array of objects, so block-only, not a library component): a 1 / 2 / 3-column grid of square portraits with name, role and a `+`, each card a real link to `/about/<slug>` (the slug is the name through pathauto's alias cleaner, `icon_site_team_slug()`); one native `<dialog>` holds every profile. `js/team-profiles.js` (`icon/team-profiles`) opens the dialog on a card click and pushes the member's address, the X / a click beside the panel / Escape close it and push `/about` back, the arrows (and ArrowLeft / ArrowRight) step through the members with the address, the title and the counter following; Back and Forward replay it all. A deep link works because `TeamProfilePathProcessor` (inbound, priority 300, ahead of the alias processor) rewrites `/about/<published slug>` to `/about` before routing and sets `_disable_route_normalizer` on the request (the Redirect module's opt-out; without it the normaliser 301s the slug away) — the browser keeps the address, the script reads it and opens that panel with `replaceState`; an unknown slug stays a 404. Order is the Order field on the member form for now (no drag panel yet). `scripts/team-content.php` seeds the six leaders from `sample-content/team/` (the live site's bios); `scripts/about-page.php` places the block where the old Leadership prose was.
- **The panels work from the keyboard**: a row's handle is a named `role=button` — arrow keys move the row, Home / End to the ends, a live region announces the position; the project picker is a combobox over a listbox (arrows, Enter, Escape returning focus). The skip link (`.skip-link`, every shell) shows as a pill on focus.
- **Editors, not admins**: the Content editor role can create and edit News, Work, Hero slides and Offices, use the media library, edit Canvas pages and the social-links menu, and holds icon_site's two scoped permissions — `manage icon lists` (Client logos, Icons, Offices, the marquee's order) and `manage icon footer`. The panel's pin / promote / remove / reorder routes are POST-only with a CSRF token and save a new revision each time. The page shell exposes `{% block content %}` for 403/404 templates.
- **The news stories are mirrored from iconagency.com.au**, not invented: `drupal/scripts/news-mirror/` (`extract.py` reads a downloaded story page into title, date, category, tile, banner and body blocks; `import.php` creates or updates the News item by title, then alias, with the old site's path as the alias, the original files behind the old site's image styles as Image media, a hosted mp4 as a Work video paragraph — the news body allows `work_video` for this — and a YouTube film as a News article video). Every story from 2020 on is in — 270 — with the old site's YouTube and Vimeo films as News article video paragraphs (their oEmbed URL comes from the page's inline Apollo state, `extract.py` reads it) and its hosted mp4s as Work video paragraphs with the old site's poster as the cover; the twelve older stories (2012–2019) were left. `import.php` reads its own folder (`articles.json`, the media, `all-urls.txt` — the old site's every story path, so a story mirrored in an earlier run is never unpublished as a sample). The invented sample stories from `news-sample-content.php` are unpublished, not deleted.
- **The form is only its fields (16 Sep 2026; user call: "Remove the top line of the form. Also I dont need Lead and intro as I can use the Content component").** The Contact form block has no settings of its own any more: `lead` and `intro` are gone from `ContactFormBlock` (no `blockForm`/`blockSubmit`/`defaultConfiguration`), from the `block.settings.icon_contact_form` schema (still FullyValidatable, an empty mapping), from the `contact-form` SDC (props `{}`; the `form` slot stays) and from the seed (`scripts/contact-page.php` now places a Content component with the old lead and intro above the block in column 1). The frame's `data-rule="top"` and the `.contact-form[data-rule]` hairline are gone too, with `.contact-form__lead` / `__intro`; `templates/contact.html` and the index demo show a `.content-page` block above the form instead. Canvas versions: the block component's active version moved (`e93eab3db9501656`; the old `9e3ff8a7778744af` is kept in the export for instances that still point at it) and the SDC's likewise; the local Contact page's placed block was repointed to the new version with the two settings stripped from its inputs (a script as user 1, and its stale autosave draft deleted) — the host's needs the same after `cim`, else its stored `lead`/`intro` fail the FullyValidatable schema on the next save.
- **The form opens on a question: Business enquiry or Careers (16 Sep 2026; user ask, after the old site's careers modal on iconagency.com.au/people-culture).** The old careers form is a Webform (`job_contact`) on the old backend, sent over GraphQL and mirrored into a HubSpot form (`0c6dbdc9-6aad-405d-bf58-dd99f3c3764c`; the business form is `43a46c67…`): first name, last name, email, phone and a required message — who receives it is in that webform's email handler, not visible from the front end. Here: a second core Contact form, **Careers** (`contact.form.careers`, recipients melbourne@iconagency.com.au to start — Content → Contact forms sets them, the reply and the message shown, as for Contact), with the same first name / last name / phone fields (field configs on the `careers` bundle, the storages shared), core's email and message, no attachment; form and view displays copied from Contact's less the attachment. The block (`ContactFormBlock`) builds both forms into the `contact-form` SDC's two slots (`form`, `careers`; a form whose config is not in yet renders as nothing) and the twig opens on a fieldset — "What is this about?", two radios outside either form, their squares the news checklist's (`.contact-form__topic`, `__option`, `__radio`) — with the CSS showing the panel for the answer ticked (`.contact-form:has(.contact-form__radio[value=…]:checked) .contact-form__panel[data-topic=…] { display: none }`; where `:has()` is unknown both show, stacked). The `topic` prop (business/careers, default business) is the answer ticked at render: the block passes careers when the request is that form's POST (`form_id`), so a careers form back with errors is not hidden behind the business one (verified: a bad careers POST re-renders with Careers ticked and the errors shown). One dress for both: `_icon_site_contact_form_dress($form, $message_label)` from the two `hook_form_FORM_ID_alter()`s ("Tell us about your project" / "Tell us about yourself"); the entity builder's subject is "Careers enquiry from …" for the careers bundle. `templates/contact.html` and the index demo carry the question and both forms.
- **The Digital page, rebuilt from iconagency.com.au/digital (16 Sep 2026; user ask: "create a new page for Digital … For the Digital services section and the listings, add the listing in four column content rows … link main menu Digital to the new page").** `scripts/digital-page.php` (idempotent, `ICON_SEED=1`) finds the page by `/digital` or the `/services/digital` it wore first (page 8 locally, seeded 11 Sep and never edited, so it was reset in place) and builds: a dark Masthead on the page's own statement ("Transformation happens here. / Human first. Innovation always."), Content ("Better user experiences. Making it human." over the intro), Content (Who we are), a Pull quote (upright), Content (Digital services), the eight services **three to a row** in three Columns (gap normal, no box; each column a Content block styled as Matt styled the first in the editor — an h4, the line and a plain list in the Small paragraph style, the slash list's rules having drawn the question "where did these lines come from?"; four to a row at first, then "3 column looks better" — the old page's "See our … services" cross-links are left out, their pages not being here), Content ("Latest work from digital") and the **Work: latest** rail. The old page's `<title>` ("Website Development Agency in Melbourne, Sydney & Canberra | ICON") has nowhere to live — the page's title is its label — so the title is Digital; its meta description is the page's Description. The alias is `/digital` (the old site's), with `/services/digital` kept as a second alias so the homepage intro's Digital link and the Expertise drawer still land here; the script also points the Main navigation's Digital link at `/digital` (it went to `/services#digital`). Pictures are the editor's to add. And `icon_site_page_attachments()` now writes a Canvas page's Description as `<meta name="description">` and `og:description`, its title as `og:title` (Canvas stores the field but wrote no tag; About and Contact had none until now) — cacheable by the page.
- **Masthead split (16 Sep 2026; user ask: "a new masthead option, like the two column news, but with our H1 header on the left and optional block above and below? Block on right to add whatever").** `components/split-masthead` (Mastheads section; `.page-header--split`, at the end of `src/components/page-header.css`): the news article's head worn by any Canvas page — the same half-and-half grid from 768px, the same air under the wordmark, the heading left in the article headline's serif (`--voice-headline-article`, book, 24ch, balanced) — with three slots: **Above the heading** (a Content block with a back link or a line of meta; the article's air under it), **Below the heading** (closes the left column on the right column's baseline, `margin-top: auto`, as the article's date row does) and the **Right column** (anything: a Picture, a Video, a Pull quote, a Content block). Props: the heading, its level, and the same Opens on as the Masthead — `_icon_canvas_page_opening()` (icon.theme) now reads either masthead for the `<html>` class, and the twig names the handover class for js/theme-handover.js the same way. Blocks placed in it drop their own gutter and framing and start left; empty slots stand in the editor (a line's height above and below, a tile's beside). Its Canvas config carries the host's UUID, as every component's must (deploy the code, shape the library on the host, read the uuid, export locally). Also fixed: `ContactFormBlock::form()` passed the built form straight into `render()`, which takes its array by reference — a PHP notice on every Contact page for anyone who sees notices (user report); the form is a variable first now.
- **Spacer (16 Sep 2026; user ask: "you know how we have line, can you add a spacer to layout").** `components/spacer` (Layout section, beside the Line; `src/components/spacer.css`): empty height between two blocks on the page's spacing scale — Small (`--space-sm`), Normal (`--space-md`, the default), Large (`--space-lg`), A section's air (`--space-section`) — adding to the flow gap around it; draws nothing, `aria-hidden`. Its Canvas config carries the host's UUID, as every component's must.
- **Work: latest is a block with its heading and its feed chosen (16 Sep 2026; user ask, on the Digital page: "Drop it and allow me to page edit this heading (this can be the default), also for the feed, I want to be able to select what is in the feed (i.e. Digital)").** `WorkFeedBlock` (`icon_work_latest`, Listings section; settings `accent` / `caps` — "Latest" over "Work" to start — `category` (the work category field's own list, or all) and `count` 1–6; schema `block.settings.icon_work_latest`, FullyValidatable) embeds the work View's `latest` display, which now inherits the default display's category argument (`'all'` is its exception, every work item), and sets the heading and feed on the display as an option (`icon_heading`) that `icon_preprocess_views_view()` reads back for `views-view--work--latest.html.twig`; More goes to `/work?category=…`. The bare Views block (`block.views_block.work-latest`) is off the library (`$hide`), its config kept for anything still placing it; it renders the defaults. The Digital seed places the block with "Latest / Digital work" over the **Websites** feed — the work category list has no Digital; the panel offers the list, and a category with no work shows no rail at all (the template renders nothing when the rows are empty) — and the Content heading that doubled the lockup is gone.
- **The split masthead speaks any heading voice, with an eyebrow (16 Sep 2026; user ask: "make it so I can select any of the H1s styles we have. Also the default text, can you make it more generic about the H1 heading. Also the eyebrow text (can that be an option above)").** `split-masthead` props: `eyebrow` (optional; the Masthead's accent line, `.masthead-accent` in `.page-header__split-eyebrow`, inside the heading over the text) and `style` — `serif` (the article headline, default), `caps` (`.page-header__lockup-caps`'s recipe: `--voice-display`, 900, uppercase) or `sentence` (`.page-header__heading--display`'s: `--voice-headline-lg`, bold, `--voice-display-lh-sentence`) — as `.page-header__split-title--<style>` on `.page-header__split-text`; the heading's example is now "The page's heading, in a line or two". The component's active version moved; the old version stays in the export for the instances on it.
- **Heading 2 — Large, the H1's size (16 Sep 2026; user ask: "add a Large H2 option style that is the exact size of the H1").** The Content block's Style menu offers "Large (the H1 size)" on a Heading 2 (`editor.editor.canvas_html_block`, `<h2 class="is-large">`; the format allows it): `.content-page h2.is-large` (base/prose.css) is the Masthead's caps line to the token — `--voice-display`, 900, line-height 1, `--tracking-caps`, uppercase — measured identical to `.page-header__lockup-caps` and the split masthead's caps style on one page (100.3px, 900, uppercase at the test viewport).
- **Uploads with spaces in their names, and the video Poster's "Oops" (16 Sep 2026; user reports: "whats wrong here?" — "This value should be a valid URI" on a poster called "gigi old vid.png" — then "Oops, something went wrong" twice).** Two faults. (1) Drupal ships file name sanitising OFF and refuses a file address with a space in it (the `uri` field's primitive-type check: `public://posters/gigi old vid.png` fails, `gigi-old-vid.png` passes — reproduced by validating File entities); `file.settings` now transliterates, replaces whitespace and non-alphanumerics with `-` and collapses doubled separators, case kept, so "gigi old vid.png" is stored as "gigi-old-vid.png". Existing files are untouched. (2) Core media library bug: a video's Poster is an image field with an upload button of its own inside the created item's form; pressing it re-runs `FileUploadForm::validateUploadElement()` on the MAIN upload, which reads `$values['fids']` from a value the poster's button never set — `count(null)` TypeError, the ajax fails, "Oops". `icon_site_form_media_library_add_form_alter()` swaps that `#process` callback for `_icon_site_media_library_validate_upload()`, the same check with the guard.
- **Inline form errors (16 Sep 2026; user ask, after a media dialog's Save did nothing because its required "Folder to save in" was empty and the error sat above the fold: "can you add a error message so the user knows?").** Core's `inline_form_errors` module is on: every form puts a field's error under the field itself (Claro's `.form-item__error-message` in the admin and the Canvas dialogs, `.form-item--error-message` on the front) as well as the summary at the top. The contact form dresses its own (`contact-form.css`): the meta voice in the brand blue under the field, the field's hairline filling with the same.
- **The media dialog's two Saves (16 Sep 2026; user report, a video that would not save: "am i missing something").** The dialog held TWO Save buttons: the add form's own, under the Poster section and off the bottom of the scroll, which saves the item and selects it, and Canvas's blue one in the footer, which inserts the SELECTION — with nothing selected it did nothing, quietly. Reproduced in the library outside Canvas: the form's Save saved (media 627, then deleted). `media_library.settings: advanced_ui` is now on, which is core's fuller add form: "Save and select" and "Save and insert" — the latter saves the item and puts it in the field in one press, no second button — and the added-item form shows the buttons where the item is.
- **The page move stays out of the editor (18 Sep 2026; user report, the Contact page's Canvas preview: "Why is this page flashing?").** The editor and its preview render the page in an iframe they re-fill on every change (`about:srcdoc`); each refill is a navigation between two same-origin documents that both carry `@view-transition { navigation: auto }`, so the whole move — the disc, the masked lockup — played inside the editor on every refill, and half-skipped ("Transition was skipped" in the console). `js/page-transition.js` now checks, before the first render, whether the document is framed (or marked `data-canvas-preview`) and if so appends `@view-transition { navigation: none }` — a later rule wins — and plants nothing. The site itself is unchanged.
- **Paragraph — Large, Normal, Small (18 Sep 2026; user call, settling an earlier rename: "a large, Normal and Small paragraph option. Normal should be the default. Currently there is no way to select normal once you select another style").** The Style menu lists three paragraph sizes. **Normal** is the plain paragraph: `<p class="is-normal">` has no CSS rule on purpose — a CKEditor style must carry a class — and picking it takes Large, Small or Caps off through the one-style plugin, which is the way back to body text; a new paragraph is Normal without the class. **Small** is `is-small` again by name (the Digital services and the team bios carry it), kept at medium weight (`--font-weight-medium`) from the same day's call "up the font weight by one" — and **the plain paragraph is medium too** ("I want the Normal to be the same font weight as small"): `.content-page p` and the lists, so body copy across the site (news, work folios, every Content block) steps up from book to medium. The format allows `is-normal`.
- **The split masthead's sentence-case leading (18 Sep 2026; user call: "Increase the line height for this Display version only").** `.page-header__split-title--sentence` is `--voice-headline-article` (0.9 of the large register; "a tad smaller") at a leading of 0.95, not the News masthead's `--voice-headline-lg` at `--voice-display-lh-sentence` (0.72), which is cut for two short lines; the masthead itself is untouched. And `style` is now a required prop (the twig still defaults it to serif), so the pick has no "- None -"; the component's active version moved again — instances on earlier versions keep their own definitions.
- **The split masthead's caps a tad smaller (18 Sep 2026; user call: "the Masthead H1 caps font needs to be a tad smaller too").** `.page-header__split-title--caps` is 0.85 of `--voice-display`, capped at `10cqw` of the intro column (`container-type: inline-size`): the lockup runs the page's width, the split heading half of it, and at the full register "TRANSFORMATION" (9.55em wide in Kumbh 900) ran past the column at a tablet width — the cap keeps the widest word inside the column at every width. The Masthead and the Large H2 keep the full register.
- **The split masthead's eyebrow tracks as the Masthead's accent does (18 Sep 2026; user catch: "This eyebrow seems to have different letterspacing to this, when they are the same").** Same class, same size token — but the Masthead's accent sets no tracking and inherits its heading's, and an em tracking inherits as the pixels it computed to on the heading: `.page-header__heading`'s -0.025em at its own `clamp(3rem, 6vw, 4.5rem)` is -1.8px at 1467 wide, a tighter -0.038em on the accent's own 47.6px. The split heading set none, so the eyebrow ran at normal tracking. `.page-header__split-eyebrow` now carries the same pixels, `calc(clamp(3rem, 6vw, 4.5rem) * -0.025)`; measured equal on both.
- **Eyebrow and Heading, in every masthead (18 Sep 2026; user call: "Call them Eyebrow in mastheads, I think one is called Title. For consistency").** The Masthead's panel labels are Eyebrow (was Accent line) and Heading (was Headline), as the split masthead's; the prop keys (`accent`, `caps`, `title`) are unchanged, so nothing placed is touched. The Work masthead keeps Client and Project name — those are what its two lines are. The Masthead's active version moved (the label is part of the schema); earlier versions stay in the export.
- **A video with a Poster would not save inside the Canvas editor — Canvas drops `update_build_id` for forms that are not its own (18 Sep 2026; user reports, twice: "Its not saving"; nothing in the log, no error on screen).** Found by capturing the press: the POST was right (op "Save and select") but carried a build id the server had no record of, while the item sat in the form store under another id. Cause: in the editor page Canvas overrides `Drupal.AjaxCommands.prototype.update_build_id` (`canvas/js/ajax.command.customizations.js`) to look the form up by the `data-form-id` marker on the matching `form_build_id` input and simply `return` when there is none. Every ajax rebuild gives the media library add form a new build id; the video upload replaces the whole form, new input included, so it survives, but the Poster's own upload replaces only its widget and relies on `update_build_id` — dropped — so the input keeps a dead id, Save posts it, the server builds a fresh empty form, finds no such button and falls back to the first one (Upload), answering with an empty add form aimed at a wrapper that is not there. Outside the editor (the node form at `/node/N/edit`, the library on its own) the same steps save. Fix: `icon_site_form_alter()` puts `data-form-id` (the form's id) on every form's `form_build_id` input that has none, so Canvas sees a form that is not one of its two and calls core's original. Worth reporting upstream: the override should fall through to the original when it finds no marker.
- **The listing filter chips speak the nav's voice (18 Sep 2026; user catch on /work: "Are the filter fonts bigger than the nav fonts? They should be the same").** `.listing-band .filter-bar__chip` is `--voice-label`, the token the header pill's links use from md up (`--nav-voice`), with the base chip's 0.05em tracking taken off — it had a clamp of its own that measured 13.9px beside the nav's 12.5px. Both landings.
- **A Line in the split masthead runs its column (18 Sep 2026; user catch: "The lines should be 100%, currently padded in").** `.page-header--split .divider` drops the Line's page gutter, as `.columns .divider` and `.page-header__inner .divider` do — the masthead is on the gutter already.
- **The Result stats label is the Normal paragraph's font (18 Sep 2026; user call: "Can the label be the Normal paragraph font").** `.work-stats__label` is `--voice-paragraph` at medium — the body copy's size and weight — where it was Paragraph Small's size by an earlier call; the tighter 1.35 leading stays.
- **The Creative page, on the /digital pattern (18 Sep 2026; user ask: "create a page using https://iconagency.com.au/icon-creative follow our /digital pattern. download and use the videos … add filler images stats blocks for the film strip this is the /creative page on our site").** `scripts/creative-page.php` (idempotent, `ICON_SEED=1`) builds `/creative` as Matt built /digital in the editor: a split Masthead (eyebrow Creative, caps "Unmissable ideas. Undeniable impact.", opens dark, a Line above and below, a square Picture with parallax beside it), a Filmstrip, a Pull quote, a wide two-column Picture and copy (h3, Small paragraph, h4, Small paragraph), a Line, "Creative services" over the five services three to a row (each a Content block: h4, the line and a plain list in the Small style, Spacers between), a closing Pull quote and **Work: latest** as "Latest / Creative work" on the `creative` category, six shown. The old page carries ONE film, its hero loop (`website_V14_SHORT LOOP_1.mp4`, 24.7 MB, 1920×1080); the script fetches it and its poster still from the old site's files the first time it runs and saves them as the video media "ICON Creative loop" (Content folder), found by name after that — so the script builds the page on a host too, with no file going through git. The Filmstrip's photos are filler from the library's team folder (work images if a site has none) and its fact cards are the About page's own figures — 2002, 5 disciplines, 90+ agencies — with library icons found by name; both are for the editor to replace. The old page's "Read more about …" cross-links are left out, their pages not being here. Meta description from the old page, sentence-cased; the script points the Main navigation's Creative link at `/creative` (it went to `/services#creative`).
- **Testimonials (22 Sep 2026; user ask: "a new testimonials component. i want it to be reusable. Maybe there is an option to have a single testimonial (hide the left right arrows and the 01 / 06 and line for single). Look at the profile left right and line etc to reuse components. Logo bottom left. Also make the 01 / 06 font bigger like the testimonial. Align with the Person's name below … Set it up so people can add remove existing quotes to the testimonial carousel (Person name, company name, Logo, recolour to make monochrome, and quote)").** Two components in a new **Testimonials** library section: **Testimonials (group)** (`components/testimonials`; `src/components/testimonials.css`, `js/testimonials.js` as `icon/testimonials`; one prop, the accessible label; slot `quotes`) and **Testimonial (item)** (`components/testimonial`; props `quote` (required), `name`, `company` ("Role and company"), `logo` — `x-icon-media: logo`, a new kind in `icon_site_canvas_storable_prop_shape_alter()`: the media library on its Logo and Image tabs — and `monochrome`, on by default). The group is the team panel's bar reused — the arrows leading, THE hairline under, the counter in the Paragraph voice over the quote column's left edge — over one quote at a time: the person (name semibold, company muted) on the left with the mark at the foot (`margin-top: auto` in a stretched column), the quote on the right in the mid voice at book weight. The quotes stack in one grid cell (the frame is the tallest quote's height; the others are `aria-hidden` + `inert`); the current one fades and rises in (`.is-showing`; reduced motion cuts). The script counts `.testimonial`s (not children — the editor's placeholder), writes `01 / 06`, steps with wrap on the arrows and the arrow keys, and sets `data-count`: one quote hides the bar. In the editor's frame every quote shows stacked (`.is-editing`). **One colour**: the mark as a CSS mask over `currentColor` (`--logo` inline, the one inline value the verify gate allows), so any logo reads as the page's ink on any ground; off, the image as uploaded. `icon_site_media_source()` now returns a file-source item (a Logo, an Icon) as its file, so `icon_media()` serves the mark. Both Canvas configs and the new folder carry the host's UUIDs, as every component's must.
- **The testimonial's mark is the scroller's size (22 Sep 2026; user call: "can the logos be the identical size rule to the logo scroller logos").** `.testimonial__logo` carries the clients marquee's stage (`--client-stage-h: clamp(5rem, 7.7vw, 9.5rem)`, home-c.css `.client`) and its bounds — 72% of the stage's 2.05:1 width, 58% of its height, the mark fitted at its own ratio — left-aligned; the mono mask fits the same box.
- **Testimonials in a box, the quote in Miller (22 Sep 2026; user call: "can the testimonials be placed in Column box by default. so the group look like this. The quote can be in the Miller font").** The group has a required `box` prop — light grey (the default then), white, black, blue or none — and wears the Columns box's own markup around the carousel (a one-column `.columns--box` with the theme class and the chameleon pair, so columns.css applies unchanged); `.testimonial__quote p` is `--font-serif`. The component's active version moved; instances on the first version keep their own definitions.
- **Testimonials: custom colours, and the mark always one colour (22 Sep 2026; user call: "add the ability to change the colours (default like this with override foreground and background. also the logo should never be colour, so remove that toggle").** The group's Box gains Custom with `bg` / `ink` hex props, the Columns box's chameleon pair (light grey stays the default); the item's `monochrome` prop is gone and the mark is always the mask over the box's ink. Both components' active versions moved; instances on earlier versions keep their own definitions (an old `monochrome: false` is simply ignored — the twig always masks).
- **The testimonial's Quote is long text (22 Sep 2026; user catch: "i think I cant type in this. Copy paste worked?" — the box was a plain string field at Canvas's 255-character cap and the quote had filled it).** `pattern: '(.|\r?\n)*'` on the prop is Canvas's cue for `string_long` with a multi-line box and no cap (`JsonSchemaType.php`); the item's active version moved, and a quote placed on the earlier version keeps its 255 characters until the item is re-saved from the new panel.
- **A testimonials box inside a Columns box gives way (22 Sep 2026; user catch: "can you remove the double padding").** `.columns--box .testimonials__box` drops its padding, ground, corner and gutter and inherits the outer box's raw role tokens (`--background`, `--foreground`, `--border`, `--icon-*`: `inherit` beats its own theme class), so the outer box is the box and its colours flow through; the carousel's own Box pick only matters on the page.
- **The team panel's counter over the name, in the carousel's voice (22 Sep 2026; user call: "The 01 / 06 should left align with the content i.e. the H2. And should be the same font as the testimonials 01 / 02").** The counter leaves the arrows' group for a ledger (`.team-overlay__ledger`) with the close; from 1024 the bar is a grid on the panel's own two columns (5fr / 7fr, the same gap), so the ledger — the counter at its start, the close at its end — sits over the text column on the name's left edge; below that the ledger runs the rest of the bar after the arrows. Both counters — `.team-overlay__counter` and `.testimonials__counter` — are the profile card's job title font (`.team__role`: `--voice-meta` at book weight; "make sure the 01 / 06 in both cases is the same font as Profile job title"), "01 / 06" with the total muted.
- **Testimonials are content, picked in the panel (22 Sep 2026; user ask: "Ive added a couple to the homepage. Is there a way I can easily select previously added and the ability to add new").** A **Testimonial** content type (`scripts/testimonial-type.php`, idempotent: Name as the title, Role and company, Quote — long text, required — and Logo, a client logo or a picture; not moderated, like Offices and Team members) and a **Testimonial (item)** block (`TestimonialBlock`, `icon_testimonial`, setting `testimonial` = the node id, schema FullyValidatable) that replaces the typed-per-page SDC in the Testimonials section: its panel is a pick of every published testimonial, newest first, "+ Add a testimonial" (the node form in a dialog over the editor — `_icon_site_panel_node_form_alter()` now takes the `testimonial` bundle — and the new one is picked at once: `panel-sortable.js` adds its option to `select.icon-panel__pick`, sets it so Canvas autosaves, and reloads) and "Edit this one" for the picked quote, which changes it everywhere it is placed. The block renders the `testimonial` SDC from the node's fields; the SDC is off the library (`$hide`) but keeps rendering the quotes placed on it before. The Home page's two typed quotes were turned into content and their placements swapped to the block.
- **Columns in thirds (22 Sep 2026; user ask: "a new 2 column option with 1/3 first column and 2/3 2nd. Plus a 2 column 2/3 + 1/3 version as well").** A required `split` prop on Columns — Even (default), One third + two thirds, Two thirds + one third — that applies to two columns (`.columns--2.columns--split-narrow-wide` is `1fr 2fr`, `--split-wide-narrow` `2fr 1fr`, from 768; three and four stay even). Required, so the pick has no "- None -" — and because Canvas validates every placed instance against the current schema whatever version it sits on, `scripts/columns-split.php` fills `split: even` into every Columns without it: every Canvas page (published and open drafts), every Work article's Canvas body and the case study pattern. Run it on the host after the deploy, before anyone publishes.
- **The carousel counts its quotes through the block wrapper (22 Sep 2026; user reminder: "if 1 then the controls and 01 / 01 and the line are not needed").** With the quotes as Testimonial (item) blocks each renders inside Drupal's `div#block-…`, so `.testimonials__track > .testimonial` found nothing — no count, no stepping, the bar up on a single quote. The script counts `.testimonial` anywhere in the track and the CSS stacks the track's children (quotes or wrappers, not the editor's placeholder) in the one cell.
- **The testimonial quote a tad bigger (22 Sep 2026; user call: "Can the Miller quote font be a tad bigger").** `.testimonial__quote p` is `calc(var(--voice-mid) * 1.125)` — Miller sets small on its body, so an eighth up reads level with the Kumbh mid voice.
- **Larger logo (22 Sep 2026; user ask: "Sometimes the logo looks too small with the height and width rules. can you add bump up logo size option or tickbox?").** A "Larger logo" tick on the Testimonial content (`field_testimonial_logo_large`, in `scripts/testimonial-type.php`), passed by the block as the SDC's `logo_large` prop: `.testimonial__logo--large` scales the scroller's stage by 1.5, so the mark's box is half again each way — for a tall or square mark that sits small in the wide box.
- **Result stats fill their column (22 Sep 2026; user catch on About: "this should be 100% of the column").** `.columns .work-stats` drops the band's prose measure (`max-width: none`) as it already dropped its gutter — the column is the measure, as for every block a column holds.
- **Padded columns, added and removed the same day (22 Sep 2026; "add a toggle for column padding so its not edge to edge", then "remove this feature").** A "Pad the columns" tick on Columns lived for an hour; gone from the schema, the twig and the CSS, nothing placed had used it.
- **The testimonial's mark sits bottom-left of its box (22 Sep 2026; user call: "Make sure the logo sits bottom left always").** The mask (and an image's `object-position`) is `left bottom`, not `left center`: a mark fitted to the box's width leaves air above and below, and that air now sits above, so the mark's foot is the column's foot whatever its shape.
- **The page move is the simple fade again (22 Sep 2026; user call: "put back the simple one with the background transition, text fade out and new page build in. locked logo/navigation as is").** `src/utilities/page-transition.css` and `js/page-transition.js` are the 15 Sep versions (commit 63b305c): a crossfade when the grounds match, a fade through the new ground when they change, the wordmark and the pill named so they hold their place; the framed-document opt-out for the editor is kept. The disc-and-lockup wipe (387feaa…ef171e3) is gone from the code; git holds it.
- **The host became a copy of local (22 Sep 2026; user call: "push everything to upsun including the database. Override the old Upsun").** A restorable backup of icon-build was taken first (`upsun backup:create`, 05:15 UTC). Then: the code (two commits), the local database imported over the host's (`upsun sql < local.sql`, 26 MB), `drush cim` on the host to drop the local-only devel and stage_file_proxy the database carried, and the files streamed as a tar over ssh (adds and overwrites, never deletes) leaving out the 17 news-story mp4s (1.4 GB — the host's files disk is 974 MB) and Drupal's regenerable `styles`, `css`, `js` and `php` folders. The standing "never push the database or files" rule was overridden for this one push, by name.
- **The mark at the frame's foot (22 Sep 2026; user call, arrow at the box's bottom-left: "I meant bottom left").** The track's children — quotes or block wrappers — are grids, so the quote inside stretches to the cell (`min-height: 100%`): the frame is the tallest quote's height, and a shorter quote's left column now runs the whole frame, its mark on the box's bottom-left, not the quote's foot.
- **The Subscribe pill fits its hint (22 Sep 2026; user catch: "Make sure this is always wide enough to be able to read Subscribe and Your email").** Its resting width was a fixed 13rem while the hint runs at 1.35× the label voice, so on a wide screen "Your email" was cut to "Your em". The rest width is now the hint's own measure — 7.2 of the pill's em for the word and the input's padding, plus the two glyph boxes, gaps and padding — with 13rem as the floor; the open width is unchanged.
- **Larger logo in the clients scroller (22 Sep 2026; user ask: "Client scroller, can you add a Larger logo to this, without making the bounding box bigger?").** A "Larger logo" tick on the Logo media type (`field_logo_large`, in the panel's Edit dialog); the marquee block passes it and `.client__logo--large` grows the mark's box to 94% × 78% of the stage (from 72% × 58%), still inside the 1.2× card, so the card and the loop are unchanged.
- **Role and Company are two fields (22 Sep 2026; user call: "Split this so 'Role: Head of Global Supply Chain Communications' and next line add field 'Company: Schneider Electric'").** The Testimonial type gains `field_testimonial_role`; `field_testimonial_company` is Company; the SDC has `role` and `company` props, a line each under the name (`.testimonial__role`, `.testimonial__company`); the block passes both. Existing testimonials were split on their last " at ".
- **The counters and the person on the site's own voices; Testimonials open Dark (22 Sep 2026; user calls: "See the dates etc on news. the 01 / 06 on testimonials and Profile should be exact same", "See 'Cancer Institute NSW' font size. Testimonial details should be the same font size", "Default to Dark (the dark theme)").** `.testimonials__counter` and `.team-overlay__counter` are the News article's date voice exactly — `--voice-label` at medium in `--color-icon-grey` (the total still muted); `.testimonial__person` (name, role, company) is `--voice-meta`, the Work card's `.work__meta`. The Testimonials group's `box` now offers **Dark (the dark theme)** first and defaults to it (`examples: black`, the twig's fallback too); placed instances keep the box they chose. The component's active version moved with the enum order — no fill-in needed, the values are unchanged.
- **The Square tile stands down for a legacy card (22 Sep 2026; user call: "If legacy exists, then there is a tile. Therefore I shouldn't get this error" — Running Wings, layers set and no Square, would not pass the editor's review: "This value should not be null" / "Square field is required").** `field_work_tile` is optional now; `WorkHooks::entityBundleFieldInfoAlter()` puts the **IconWorkTile** constraint on it (`Plugin/Validation/Constraint/WorkTileConstraint` + validator): a violation on the field only when it is empty AND `field_work_legacy_layers` is too, so the message reads "Square is required — unless the Legacy folio card has layers". The card already preferred the layers (`work-card.twig`); the article's banner falls back from the Banner to the Square and, with neither, shows none.
- **A box in a case study keeps its padding (22 Sep 2026; user catch: a Testimonials box placed in a Work article "needs to be full width, currently cropped. And missing all its padding?").** `.work-article .columns { padding-inline: 0 }` — the frame self-pads, so the Columns drops its page gutter — was also stripping a BOX's own padding, while the box kept its page-gutter margin inside the already-padded frame (inset twice, text flush to the edge). Now `.work-article .columns:not(.columns--box)` drops the padding and `.work-article .columns--box` drops the margin instead: the box runs the frame's full width with its padding intact (work-article.css).
- **The carousel turns itself; the quotation marks are the template's (22 Sep 2026; user asks: "add an auto next testimonial, allow enough time to read", and on the Quote field's "without quotation marks": "They need to add quotation marks? Or we automatically put them in?").** `js/testimonials.js`: each quote holds for its own reading time — a seven-second floor, then a third of a second a word — and only while the section is in view (IntersectionObserver at 40%), the pointer is off it, nothing inside has the focus and the tab is showing; an arrow or an arrow key restarts the clock from the quote chosen; reduced motion turns nothing; one quote never turns. `testimonial.twig` wraps the words in typographic marks (`&ldquo; &rdquo;`) and sheds any typed at the edges — straight or curly — so the older quotes are not doubled; the Quote field's and the SDC prop's descriptions now say "The quotation marks are added for you — leave them out."
- **The news article's Add Block order (22 Sep 2026; user ask: "order this Content / Pull quote / Image / Video embed (YouTube, Vimeo) / Video hosted").** `field.field.node.news.field_news_content`: the paragraph types' `target_bundles_drag_drop` weights (prose 1, pull_quote 2, news_article_figure 3, news_article_video 4, work_video 5) and `target_bundles` in the same order — the Paragraphs widget's dialog lists them by that weight.
- **Testimonials arrive like the rest (22 Sep 2026; user check: "the scroll skew and load animation is inline with the rest of the site?").** The group's outermost element — the box, or the carousel when there is none — carries `data-animate`, the fade-up the pull quotes and the stats band wear (js/reveal.js + utilities/animations.css, `.is-visible` on scroll-in, inert under reduced motion). No velocity skew, on purpose: in a case study only the media tilts (`--wa-skew` on the banner, figures, pictures, films and the Next-up cards), never a text block.
- **One quote keeps the bar (22 Sep 2026; user call: "When there is only one quote, can you replace the arrows with a little quotation mark. And keep the 01. And keep the line?").** Reverses the earlier "one quote hides the bar": on `data-count="1"` the arrows and the "/ 01" go, a `.testimonials__quotemark` (an `&ldquo;` in Miller at the mid voice, always in the markup, shown only then) stands on the column's edge in the arrows' row, and the hairline and the "01" stay.
- **Mobile: the pill back at the top, the wordmark under it; the hero's client line (22 Sep 2026; user call: "on Mobile, can you place the nav back up the top. Move the logo under it. Make the client link left aligned, and same size font as the services links").** site-header.css: below md `.site-nav` is fixed at `top: 0.75rem` (centred, frosted, the EXPERTISE drawer dropping DOWN like the desktop's — `.site-nav__pill` is `column` at every width now), `--nav-voice` and a measured `--nav-pill-h` (3.7em of it) live on `.site-header`, and `.site-logo` sits at `0.75rem + pill + 0.75rem`, CENTRED (`left: 50%; translate: -50% 0`, the gutter and `translate: none` from md — user call, later that day: "centre the logo and the client link on mobile", the size kept), `z-index: 9999` so the open drawer passes over it (10001 again from md); `--nav-clearance` is 0 at every width (the token stays for a bottom bar's return). `themes/custom/icon/src/admin-chrome.css` mirrors both tops plus the admin UI's offset — it re-states them and wins by order, so a change in site-header.css must be made there too. home-c.css: `.hero__client` (and the build counter `.hero__count`, its stand-in) is centred between the gutters below md and left at the gutter from md, in `--voice-mid` (the expertise links' voice) at every width.
- **The lockup masthead's caps fit the phone (22 Sep 2026; user catch on About: INTERDISCIPLINARY / GROUNDBREAKERS ran off the screen).** `.page-header__heading--lockup` is a size container and `.page-header__lockup-caps` is `min(var(--voice-display), 9.8cqw)` — INTERDISCIPLINARY, the widest word the mastheads carry, runs 10.1em in Kumbh 900 on the caps tracking (measured), so a tenth of the heading's width per em keeps it on the page. The cap is one number for every lockup masthead (About, Work, News…): at 375px they all read 32px where they read 38px before; on the desktop the display token is the smaller of the two and nothing changes. The split masthead's caps have had the same device since Sep (their intro is the container).
- **The phone's menu: a square that opens into the pill (23 Sep 2026; user calls: "move the logo back to the top left. Make the menu a collapsed version of the menu bar with two lines (hamburger). Place it in the exact spot as the expanded menu top right, square. When it opens it should be the menu with the icon home on the right"; "no X: tap outside or a link", search "as is", the logo "scrolls away"; then "the mobile navigation looks different to the desktop? Should be identical", "smooth animation on open and closing back", "the hamburger lines should follow the nav item colour rules").** `site-header.html.twig` adds `button.site-nav__burger[data-nav-burger]` (two lines) after the list in the pill's row. site-header.css below md: `.site-nav` is fixed top-right, 0.75rem (12px) above and at both sides (user call, later: "12px left right top"; the wordmark stays on the page gutter); THE SAME PILL AS THE DESKTOP'S scaled as one piece — every padding and gap is the desktop's rem value as a fraction of the desktop's full-size type (0.75rem = 0.667em, 1rem = 0.889em, 0.5rem = 0.444em…), bare until scrolled (frosted on scroll, open, drawer, search), and `--nav-voice` is the label voice capped to fit: the open row plus the pill's padding is `--nav-row-em` (31.3, measured) wide, so a phone narrower than that between its gutters shrinks the type just enough (the desktop's row at its own paddings needs ~395px; a phone has 327). Without `.is-open` the pill shows only the burger — `padding-inline: 0.667em`, the burger square from the row's height (`aspect-ratio: 1`, stretched); with `.is-open` the burger goes and the row is the desktop's — the home glyph, the links, the search (user call, later: "the order should be identical to desktop, home icon first") — the glyph shown regardless of scroll; the open pill is the full width inside the 0.75rem insets (`width`, not max-width — "width 100% with the same padding left and right"), the row centred in it; the pill is frosted at every moment below md, the rest square included ("background always on for the mobile nav"), in the same colours as the desktop's frosted states — `--nav-bg-scrolled`, and `--nav-bg-over-hero` over a hero; the desktop's bare-over-hero reset is md-only again (for a day it applied at every width and left the phone's square with the blur alone, lighter than the desktop's — user catch, 23 Sep 2026). ANIMATION: the row is always laid out in full and the pill clips it — `max-width` eases from the square (`--nav-row-h`, the row's height measured by js/header.js on load, after the fonts and on resize, plus 1.333em) to the width between the gutters; the pill runs `direction: rtl` so the clip keeps the right end (the burger stays put, the links slide out leftwards), the row and the drawer set `ltr` back. NOTHING IN THE ROW MOVES while it plays (user call, 23 Sep 2026: the items "jump/move position"): the row is anchored right (`margin-inline: 0`, not centred), the home glyph is at its full width at every moment below md (the desktop's scroll-reveal is restored from md), the burger is `position: absolute` over the row's right end and fades rather than shrinks (the search glyph appears where it was), and the pill's right padding is the closed 0.667em in both states — the open state's 0.889em is on the left only. Visibility hands over after the ease. A 0fr→1fr grid column was tried first and stayed at nothing on a fresh page's first open. The burger wears the links' ink: it is in the `.is-over-hero` white list and hovers blue. `.site-logo` is back on the left gutter, its top `0.75rem + --nav-pill-h / 2 − 0.184 × --header-control` so the bubble's middle sits on the square's centre (user call: "move the logo down a tad, so the middle of the hamburger aligns with the speech bubble"; the bubble runs y 0–18.4 of the mark's 50-unit box); js/header.js sets `--nav-row-h` (the row's height, the burger's width — an `aspect-ratio` square did not take, so the lines sat off-centre) and `--nav-pill-h` on the HEADER, the pill's and the wordmark's shared parent (z 9999, the open pill runs over it). js/header.js: the burger opens; a tap outside the nav, a scroll of more than 40px from where it opened, a followed link (not the EXPERTISE trigger), Esc or a resize to md closes it, closing the drawer and the search face with it (`closeSearch`, handed up like `closeSubmenu`). From md every mobile rule is reset in the md block. `admin-chrome.css` mirrors the logo's and the pill's tops. The design-system page's pill demo has no `.site-nav` wrapper, so it stays the desktop row at any width.
- **The home intro's caps fit the phone too (23 Sep 2026; user catch: COMMUNICATIONS ran off at 375px).** `.intro__headline` is a size container and `.intro__headline .lockup-caps` is `min(var(--voice-display), 10.3cqw)` — the masthead's device (page-header.css): COMMUNICATIONS runs 9.6em in Kumbh 900 on the caps tracking, so a tenth of the headline's width per em, a hair under, keeps it inside (32–34px at 375px; the token wins on the desktop, 70px at 1262px).
- **The intro's logo film has a floor (23 Sep 2026; user call: "this icon video needs to be bigger on mobile and tablet").** `.intro__mark` is `max(5.5rem, 1.25 × --intro-display)` on both sides: 88px on phones and tablets (it was 48 and 54), the ratio's own value from about 1260px wide up, unchanged there.
- **Picture is "Picture (image/video)" in the library (23 Sep 2026; user call).** The SDC `name:` of `components/news-article-figure` (the one Picture everywhere) says what it takes — a picture or a looping film; nothing else changed.
- **Expertise links are a fixed block (23 Sep 2026; user call: "this section is more of a one off. I'm wondering if we set it up as a fixed component?" — and "move it to the ICON folder").** `ExpertiseLinksBlock` (`icon_expertise_links`, in the ICON folder) renders the `expertise-links` SDC with an `intro-expertise` item per child of the main menu's EXPERTISE item — the same five the navigation's drawer shows, so the list lives in the menu alone. Settings: `heading` (the label beside the list) and `gutter` (on the page gutter; off inside the intro band or a box); the panel links to the menu. The group SDC gained a `gutter` boolean prop for it. Both SDCs are off the library (`$hide`), the Expertise folder is gone; `scripts/expertise-links-block.php` swapped the typed pieces for the block — the Home intro's five items in its `expertise` slot (gutter off) and the Expertise page's group in its column (gutter on) — drafts included and restamped. `intro.twig`'s expertise area is a plain wrapper now (grid placement + the reveal delay, which the block's row inherits); the intro's `expertise_label` prop is unused and says so. Schema `block.settings.icon_expertise_links`. NEW COMPONENT: on deploy, run `canvas-library.php` on the host first, read the block's config UUID, `cset` it locally, `cex`, second commit — the standing pattern.
- **Lists: tighter lines, and a Plain list (23 Sep 2026; user call: "tighten up the line height, but keep the space between list items. Also offer a bullet version that has no slash, and left aligns with 0 left margin indent").** prose.css: `.content-page ul li, ol li` are `line-height: 1.45` (the paragraph keeps 1.75), the 0.5rem item gap as before; `ul.list-plain` is the slash list without the slash or the indent — an editor style, **Plain list** (`editor.editor.canvas_html_block`), `list-plain` allowed on `<ul>` in `filter.format.canvas_html_block`.
- **EXPERTISE opens its drawer; "Our approach" leads it (23 Sep 2026; user call: "make it that the Expertise navigation item is not a link. To trigger the dropdown, you click it. Add a new menu item to the dropdown called 'Our approach' to the top of the list, and this can link to the /expertise page").** `site-header.html.twig` renders the item with children as `button.site-nav__link` (`button.site-nav__link` in site-header.css strips the button chrome); js/header.js: a click TOGGLES the drawer at every pointer — hover no longer opens it (user call, later: "click to view dropdown, not the hover"; the `:has(…:hover)` open rules and the mouseenter handlers are gone; Esc, a click outside and the focus leaving close it). `scripts/menu-our-approach.php` (idempotent, CONTENT — run on the host too) adds the child "Our approach" → /expertise at weight −10 under Expertise. `ExpertiseLinksBlock` skips a child that links to the Expertise page itself, so the fixed list stays the services. The menu's own Expertise link still points at /expertise for anything else that reads the menu.
- **Paragraph — Small: a tad smaller, and Normal's leading (23 Sep 2026; user call: "Paragraph small line height should have the same ratio to the normal … Also can Small be a tad smaller?").** `p.is-small` is 1rem (was 1.0625rem) at `line-height: 1.5`, the Normal paragraph's ratio (it was 1.6 — larger than Normal's, which read as airier); the slash on a Small list item follows the size. Inside a list the item's 1.45 still applies.
- **Lists: more room between items (23 Sep 2026; user call).** The item gap is 0.75rem (was 0.5rem), 0.6rem between Small items (was 0.4rem); the lines inside an item stay at 1.45.
- **Every prose size scales (23 Sep 2026; user check: "making sure the paragraph and heading etc have the clamp scaling").** Audit of prose.css: the headings, Normal and Large paragraphs, the caps voices and lists were already clamps or voice tokens; three were fixed rem and now follow — Paragraph Small is `clamp(0.875rem, 73% of --voice-paragraph, 1rem)` (14–16px), the blockquote's footer and inline code are `0.875em` of their parent; the lists name `--voice-paragraph` instead of repeating its values.
- **Prose caps headings fit their column (23 Sep 2026; user catch: an H2's "EXPERIENCES." ran past a third-width column).** `.content-page` is a size container; `h2` is `min(its clamp, 10.2cqw)` (COMMUNICATIONS, 9.6em, the widest word the site carries — 11.5 was sized for a twelve-letter word and let it through) and `h2.is-large` `min(--voice-display, 9.8cqw)` — the masthead's device, so a caps word never leaves its block; a full-width block never meets the cap.
- **Paragraphs Normal and Small at 1.4 leading (23 Sep 2026; user call: "looks a bit too spaced out").** Both were 1.5; the list item's 1.45 and the Large paragraph's 1.26 are unchanged.
- **Columns: three across from 768 (23 Sep 2026; user call: the third column dropped under the two at tablet width while a nested pair held three).** `.columns--3` moved from the lg block to the md block in columns.css; `.columns--4` is still two at md, four at lg; below md everything stacks.
- **Panel props that only show when they apply (23 Sep 2026; user ask: "should [Split] be hidden or greyed out unless 2 col is selected?").** `_icon_site_component_panel_states()` (from `icon_site_form_alter()` on `component_instance_form`) puts Drupal form `#states` on the Canvas panel — its form attaches core/drupal.states, so they work: the Columns' **Split** is visible only with `columns` = 2, and a box's **Custom background / foreground** (`bg`, `ink`) only with `box` = custom (Columns and Testimonials alike). Hidden, not disabled: a required prop still validates and keeps its value. The rule map is in the function; add a pair there for the next one.
- **Paragraph gap matches the heading's (23 Sep 2026; user call: "the gap between paragraphs maybe too much. Can it match the gap between the heading and paragraph … update the Small to match but its size ratio").** `.content-page p { margin-bottom: 0.55em }` (was 2rem): 12px at the Normal size, the h4's 0.75rem, and 8.8px at Small, 0.55 of its size; Large scales up with it.
- **A split's third lines up with a three-column row (23 Sep 2026; user catch: with the same Wide gap the 2-column split and the 3-column row did not align).** columns.css names the column gap (`--columns-gap`, set by the gap modifiers) and the thirds are `calc((100% − 2 × gap) / 3)` beside `1fr` — a 1fr/2fr split gave the wide side one gap fewer than two columns hold, so its edge sat a third of a gap off.
- **Work: latest — each line optional, inside the page gutter on a Canvas page (23 Sep 2026; user asks: "add an option to hide the line above and below … needs page padding left and right", then "separate these, and make the lines within the page margins not edge").** `WorkFeedBlock` settings `rule_top` / `rule_bottom` (checkboxes Line above / Line below, DEFAULT OFF — user call later that day: "make the default lines switched off"; `requiredKey: false` in the schema, as the one-day `rules` key that is read as both; a rail with neither key has no lines — a NEW SETTING ON A PLACED BLOCK MUST BE OPTIONAL or every placement fails "is a required key", which stopped a publish); they ride on the display's `icon_heading` option, `icon_preprocess_views_view()` hands them to `views-view--work--latest.html.twig`, which sets `data-rule` to both / top / bottom / none. `.page-canvas .work-article__next { margin-inline: var(--gutter-page) }` — a margin, so the lines run within the margins; inside a case study the frame pads already.
- **The expertise pages in the Digital layout (23 Sep 2026; user call: "see this example for digital … can you do the same for /creative … then /communications, /reputation and /production").** `scripts/expertise-pages.php` (ICON_SEED, optional ICON_PAGE=creative|communications|reputation|production) builds each from the old site's words in the arrangement Matt gave Digital in the editor: split masthead (dark, divider above, picture aside), filmstrip, the intro caps line as a third beside the approach and who-we-are columns, a pull quote, "… services" beside a Large line, the services three to a row with a light-grey Featured slash-list box first (each service an h4 over a plain list of Small items, a spacer under; a service with no items keeps its line as a Small paragraph), a closing quote, Work: latest narrowed to the page's category (Production: all work). Creative (page 23) was rebuilt keeping its own masthead picture and films; Communications (28), Reputation (29) and Production (30) are new and BORROW DIGITAL'S masthead picture and filmstrip cards as placeholders until pictures are chosen. The main menu's children now go to /communications, /reputation, /production (the Expertise links block and the drawer follow). CONTENT, not config — run the script on the host after deploy, or push the database. Rerunning resets a page to the script; edits in the editor since are lost, so add new words to the spec first.
- **The split masthead's eyebrow in proportion to its title (23 Sep 2026; user call: "on the split header only, this font needs to scale down, in proportion to the H1 (also space between needs to match the H1 line height). Use the About page for the split header ratio").** page-header.css: `.page-header__split-title` names its size as `--split-title` per style (caps: the capped display voice; serif and sentence: the article headline voice) and `.page-header__split-eyebrow` is `calc(var(--split-title) * 0.58)` — About's accent-to-caps ratio, measured — at line-height 1.3 with no margin, as About's accent sits over its caps.
- **The split masthead's title sits centre-middle (23 Sep 2026; user call: "align this centre middle. And, text align centre").** `.page-header__split-title { margin: auto 0; text-align: center }` — the intro column is flex, so the title takes the free height between the above and below slots; the serif style's 24ch measure centres with `margin-inline: auto`.
- **The old site's films on the new expertise pages (23 Sep 2026; user check: "you downloaded the videos from the individual pages of the old site for these?" — not at first; now yes).** `scripts/expertise-films.php` (ICON_SEED; idempotent, films found by media name) fetches from drupal.iconagency.com.au the films the old pages carried — the film URL is the poster's path without the image-style prefix and its `.png` — with their posters, as video media (Communications loop, Communications coffee B-roll, International Women's Day, Mark Forbes, Production crop; Production's hero is the "ICON Creative loop" Creative already had), and puts each page's hero loop on its split masthead aside and its films in the filmstrip's first photo cards; the fact cards and the remaining placeholders stay. CONTENT + FILES: on the host run the script (it fetches from the old site itself) rather than pushing files.
- **The footer's eyebrow (23 Sep 2026; user ask: "add an eyebrow in this font that says 'Make what matters' above the Let's talk heading").** `icon_site.footer` gains `eyebrow` (Content → Footer, "Call to action eyebrow", default "Make what matters", empty hides it; schema updated); `site-footer.html.twig` prints it as `.site-footer__eyebrow.masthead-accent` inside the CTA link over the big words, at 0.58 of the footer caps on 1.3 leading (site-footer.css) — the mastheads' accent proportion.
- **LET'S TALK on the About lockup's leading (23 Sep 2026; user check: "Is the Let's talk the same as the H1" — the same token, weight, tracking and eyebrow ratio; only the leading differed).** `.site-footer__talk { line-height: 1 }` (was `--voice-display-lh`, 0.82), so the footer lockup and the About masthead read identically.
- **The paragraph voice reaches the 4xl horizon (23 Sep 2026; user catch: "when I scale the browser, the paragraphs are not scaling up").** `--voice-paragraph` was `clamp(1.125rem, 1.75vw, 1.375rem)`, capped at 22px from ~1260px while the mid and meta voices grow to ~2560; it is now one clamp, `clamp(1.125rem, 0.85rem + 0.6vw, 1.75rem)` (user note: "we are using clamp()") — 18px on a phone, 21 at 1260, 24 at 1730, 28 from ~2400. Small follows at 73% (cap raised from 1rem to 1.3rem); lists, forms and everything else on the token follow too.
- **The split masthead's own lines, top and bottom (23 Sep 2026; user call).** `split-masthead.twig` puts `data-rule="both"` on `.page-header__split-intro`, so the column draws its two hairlines itself (rules.css) with the above and below slots inside them; `scripts/masthead-dividers-off.php` removed the Divider components placed in those slots for the lines (pages and drafts), and expertise-pages.php no longer adds one. Every hairline on the site is `--rule-weight` (rules.css), 1px — the user asked "make all the lines 1px": they were; the only 2px strokes are the footer's office toggle glyph and the parked hamburger's bars, icons.
- **Hairlines at the full ink (23 Sep 2026; user call: "make all lines 100% of their colour").** `--rule` (colors.css) is `currentColor`, not 25% of it; every hairline on the site reads it (rules.css draws, dividers, the bars, the ruled lists). The `--border` tokens — the grey borders of fields and the like — are untouched.
- **Paragraph Small a touch bigger (23 Sep 2026; user call).** 78% of the paragraph voice, was 73% — 16.5px at 1260, 21px at the horizon (cap 1.3rem).
- **A new Masthead split opens on the caps H1 with an eyebrow (23 Sep 2026; user call: "it should default have the Eyebrow and H1 cap font, not the article font default").** split-masthead.component.yml: `style` lists caps first and its `examples` is caps (was serif); `eyebrow` carries an example ("Section name") so a new instance has one to replace; the twig's fallback is caps. The component's version moved; placed mastheads keep their own style.
- **Caps on every heading level (23 Sep 2026; user ask: "H4 should have a Caps version (there is a Caps label, but that is different) — in fact all headings should have that option").** prose.css: `.content-page :is(h3,h4,h5,h6).is-caps` — the level's own size in the H2's caps register (900, caps tracking, set solid). Editor styles Heading 3 — Caps … Heading 6 — Caps (`editor.editor.canvas_html_block`), `is-caps` allowed on h3–h6 in `filter.format.canvas_html_block`. Heading 2 is caps already (its Sentence case, Serif and Large are the alternatives); Heading 4 — Caps label stays the label voice, a different thing.
- **Service headings in Heading 4 — Caps (23 Sep 2026; user call: "update all the expertise pages where we listed services to have the Heading 4 caps, including the original digital page").** `scripts/services-headings-caps.php` (idempotent, drafts included) puts `is-caps` on the h4s of every Content block sitting directly in a three-column row on Digital, Creative, Communications, Reputation and Production — the Featured box and the intro columns keep sentence case; expertise-pages.php now writes the class for new services. Content — run on the host too.
- **A block setting must be in defaultConfiguration() to be stored (23 Sep 2026; user catch: the rail's "Line above" tick "isn't recognised" on save).** Canvas keeps a block instance's settings by the keys `defaultConfiguration()` names (BlockComponent); `rule_top` / `rule_bottom` had been taken out of `WorkFeedBlock`'s defaults to dodge the default-merge, so a tick was never stored. They are back, at FALSE (off is the default now, so the merge is harmless); `rules()` reads them plainly. And the view's render cache is keyed on the block's settings too (`#cache keys`), so two rails on one feed with different headings or lines no longer share an entry. Both lessons in LESSONS.md territory: a new block setting is optional in the schema AND present in the defaults.
- **Paragraph gap up by half (23 Sep 2026; user calls on a news article: "feels too tight", then "not double but increase it").** `.content-page p { margin-bottom: clamp(0.9375rem, 0.5rem + 0.8vw, 1.5rem) }` (user: "it should be clamp") — 15px on a phone, 18 at 1260, 24 from 2000; the same gap under Small and Large paragraphs.
- **Every masthead cascades (23 Sep 2026; user call: "can all mastheads have [the word animation] for consistency?").** split-masthead.twig: the title carries `data-reveal-words` (the About lockup's and the work masthead's word cascade, js/reveal.js) in place of the block fade-up; the eyebrow's word rises first, the title's words after. The aside picture already had the parallax reveal.
- **Pull quotes cascade too (23 Sep 2026; user call: "if the pull quotes had the same text animation as the H1 mastheads").** pull-quote.twig: `data-reveal-words` on `.pull-quote__text` (js/reveal.js's word cascade); the blockquote's `data-animate` fade is gone so the two entrances don't stutter; a cite keeps the fade. The article templates' static pull quotes in the design system are unchanged. **Reverted the same afternoon (user call: "the animation has messed with the letter spacing. Back to the animation that we had"): the per-word spans changed the serif's spacing between words, so the quote is back on the blockquote's `data-animate` fade, the text unwrapped, the cite inside the fade.** The masthead titles keep the cascade.
- **Left, centre and right in the editor (23 Sep 2026; user ask).** `editor.editor.canvas_html_block`: the toolbar gains `alignment` after Styles, `ckeditor5_alignment` enabled for left, center and right (no justify); `filter.format.canvas_html_block` allows `text-align-left/center/right` on p and h2–h6; prose.css draws them under `.content-page`.
- **The split masthead's lines, each a toggle (23 Sep 2026; user ask: "add toggles for the line below").** Props `line_above` / `line_below` (boolean, on by default — a placed masthead without them keeps both); the twig sets `data-rule` to both / top / bottom / none. The version moved; instances keep their own.
- **The split masthead's Reduced padding toggle (23 Sep 2026; user ask: "a reduce padding toggle, with less padding top and bottom").** Prop `compact` (boolean, off by default; a placed masthead without it is unchanged) puts `page-header--split-compact` on the masthead: the top clearance keeps a small step under the wordmark (`2rem + --logo-h + --space-xs`, the article's `--space-sm` and the grid's second helping of `--space-sm × 2` come off — the line sits ~26px under the logo at 1260 instead of ~87), and the column closes on `--space-sm` instead of the article's 3xl. Measured on /contact: the top line rose 61px, the next block 99px.
- **Prose sizes run to the 4xl horizon (23 Sep 2026; user check: "the offices text scale up … paragraph and headings etc should do the same?").** The offices wear the voice tokens, which grow to ~2560px; the editor's h1–h6 and Small stopped at ~1870–2000px (h2 3.5rem, h3 3rem, h4 1.875rem, h5 1.5rem, h6 1.25rem, Small 1.3rem). Same slopes, so nothing moves below the old caps; the caps are now where the slope meets 2560: h1 and h2 4.75rem, h3 4rem, h4 2.4rem, h5 2rem, h6 1.75rem, Small 1.375rem (78% of the paragraph's 1.75rem); the prose blockquote 3rem (it met 2rem at 1280, now ~1920). Measured on /contact: the h2 45px → 76px and Small 17.6 → 21.8px between 1500 and 2560.
- **Office details at the paragraph's weight (23 Sep 2026; user ask).** `.office-list__details` (address, Phone, email) is medium, as `.content-page p` is; it had inherited the body's 400.
- **Reduced padding on by default, and on across the expertise pages (23 Sep 2026; user call: "make these default please for this masthead. Turn on for the Expertise pages please").** The split masthead's `compact` example is `true`, so a NEW masthead opens with both lines and the reduced padding (a placed one without the prop stays as it was — the twig reads an unset `compact` as off). `scripts/masthead-compact.php` (idempotent, pages and drafts, drafts restamped) sets the three on the /digital, /creative, /communications, /reputation and /production mastheads — and MOVES EACH INSTANCE TO THE ACTIVE COMPONENT VERSION: a placed instance keeps the version it was placed on and its inputs are validated against that version's schema, so a page built before a prop existed refuses it ("the `compact` prop is not defined") until the instance moves up, which an editor save does on its own. Run on the host after the deploy.
